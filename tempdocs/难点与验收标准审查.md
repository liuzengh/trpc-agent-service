# 难点与验收标准审查（阶段 33）

> 用途：对照用户给出的「题目难点」与「验收标准」逐条审查“是否解决”。
> 证据口径与《需求验收证据清单》一致（file:line 代码优先；方案类条目引用文档 +
> 代码支撑点）。全部引用的基础证据见验收清单，不重复展开。

## 一、7 条题目难点逐条结论

| # | 难点 | 结论 | 证据 / 说明 |
| --- | --- | --- | --- |
| 1 | 多租户隔离不只 tenant_id，还含配置/权限/密钥/数据/日志/工具/成本隔离 | ✅ 已解决 | tenant=治理边界（tenant.go）+ AppName 数据隔离 + tool RBAC/白名单 + secret AES + governance 脱敏 + usage_records/tenant_cost；详见验收 A5/C1/C5 与《租户配置模型与数据后端路由设计》。2 期成员/RBAC 保留表规划（001 注释） |
| 2 | 节点化部署要求 Worker 尽量无状态，但依赖 Session/Memory/Summary/工具上下文 | ✅ | 无 sticky：worker 每次从消息取租户/session，共享 session/memory 后端 + Redis 会话锁串行化（验收 A3/A4/A8）；summary 由框架后端随会话重建（迁移文档边界） |
| 3 | IM 乱序/重复/超时/长度/身份映射，不等同 HTTP chat | ✅ | 幂等（msg id 去重 + 双幂等 + outbox）、会话锁定序、runTimeout/HTTP 120s、截断（limit.go）、身份映射（open_id→UserID）；差异表见《IM平台限制与降级策略》 |
| 4 | 不同后端一致性能力不同（Redis/SQL/向量/对象存储） | ✅ | 一致性取舍表 + 同步策略在 docs/存储与数据访问设计.md、多后端适配方案.md；会话域用锁+AppName 语义，knowledge/artifact 域单后端收敛（A6/A8） |
| 5 | 监控审计跨组件串联（模型/工具/知识库/沙箱/外部系统） | ✅ | trace：im.callback→agent.run→im.reply + 框架子 span；指标全埋点（C2/C3）；审计 11 字段（C4）；sandbox(code-exec)/skill/artifact 进入 usage 计量（#4 维度已接通） |
| 6 | 企业级：灰度/回滚/租户限流/成本/合规审计 | ✅ | Agent 版本回滚 + 租户配置版本回滚（D3）；IM 限流 + token 预算 + 租户成本（B5/C2）；审计保留 + 用量（C4）；灰度策略见《灰度发布与配置回滚策略》 |
| 7 | （隐含）逐项验收必须有真实实现证据 | ✅ | 见《需求验收证据清单》全文（file:line）；纸面项已全部收敛或删除（DDL 清理 8+1 张、三级 skill、成本口径等） |

## 二、7 条验收标准逐条结论

| # | 标准 | 结论 | 证据位置 |
| --- | --- | --- | --- |
| 1 | 覆盖多租户/节点化/数据同步/多后端/IM/治理监控/故障恢复 | ✅ | A3-A8、B、C、D 各组；见清单目录 |
| 2 | 数据模型表达 tenant/agent/channel binding/session/event/memory/summary/audit 关系 | ✅ | DDL 001~011（tenants/agents/bindings…）+ framework session 表 + docs/详细设计文档 §2 最小模型；冗余表已清理（阶段 33 DDL 审计） |
| 3 | ≥2 种 IM（含企微/微信）差异说明 | ✅ | 企微 + 飞书：差异表《IM平台限制与降级策略》、接入差异 B1/B3/B5；真实收发=账号联调等待项（联调手册） |
| 4 | ≥3 类后端（Redis/SQL/向量/对象存储）存储与同步策略 | ✅ | Redis（session/memory/bus）、MySQL（管理域/audit/outbox/ledger）、Milvus（KB 向量）、MinIO（artifact）：策略在 存储与数据访问设计.md §数据域总览 + A6/A7/A8 |
| 5 | 完整消息链路时序 + trace_id 贯穿 | ✅ | docs/详细设计文档.md 时序图 + C3（im.callback→agent.run→im.reply 同 trace，bus.Message.TraceID 传播）；IM 联调手册 §1 链路图 |
| 6 | ≥8 生产风险及缓解 | ✅ | docs/技术选型与风险清单.md（12 条）；D1/D2 故障降级 + 优雅关停 |
| 7 | 明确框架复用 vs 平台新增 | ✅ | docs/多后端适配方案.md（框架 session/memory/artifact/knowledge 后端复用清单）；docs/详细设计文档.md「实现对照」；AGENTS §7 决策区 |

## 三、审查发现（阶段 33 已处理或如实标注）

- 原「4 级 skill 资产/成员 RBAC/自有 memory 表」等纸面 DDL 全部收敛（删除 9 张表 +
  RBAC 预留规划），避免“建了没用”误导验收；
- 用量维度 tool/sandbox/artifact/skill 现已全部有真实触发与计量（usage_meter.go）；
- 剩余等待项均为外部条件：真实 IM 账号收发（联调手册）、K8s HPA（观测已就绪）、
  压测数值回填（容量评估指南）。
