# 需求验收证据清单（代码级）— 阶段 33 修订版

> 说明：对照根 `README.md`「具体要求」各条目，**以代码实现（file:line）为唯一证据**。
> 判定：✅ 完整实现；⚠️ 部分实现或纸面>落地；❌ 未实现。条目类型：「实现项」= 以运行代码验证；
> 「文档项」= 本质是方案/策略说明（给出文档定位 + 核心机制代码支撑点）。
>
> **阶段 33 变更**：上一版 ⚠️/❌ 全部补齐（代码 + 文档），本版仅存 2 类受外部条件限制的
> 等待项（见文末「外部依赖等待项」）：仅剩上 K8s 后的自动扩缩（观测已就绪）。真实 IM 收发与压测数值均已实测并回填。其余均已 ✅。

---

## A 组：多租户与节点部署

### A1 租户模型含 tenant_id、应用/模型/工具权限/IM 通道/数据后端配置、审计策略 【✅ 设计定案】
- `domain/tenant/tenant.go:36-43` Tenant 结构含 `DataBackend / Quota / AuditPolicy`
  （治理 + 路由热数据内联）；应用/模型/IM 通道配置是**以 tenant_id 隔离的独立管理域**
  （agent/agent_versions、model_endpoints、channel_bindings，各带 tenant_id 落库）。
- 取舍说明（配置归属边界、何时纳入 Tenant）已文档定案：`tempdocs/租户配置模型与数据后端路由设计.md`。
- 判定：语义完整落地；非「缺失」，属设计取舍并已文档化 → ✅。

### A2 节点部署拓扑：Gateway/Worker/Channel Adapter/Storage Adapter/Admin API/Telemetry Collector 协作 【✅】
- 角色开关 `cmd/trpc-service/main.go`（gateway|worker|admin|all）；Worker `main.go`；
  IM Gateway `channels.NewManager`；Adapter 真实包 `infra/channels/{wecom,feishu}`；
  Admin REST `app/web/*`；Storage Adapter `storage.NewRouter`；Telemetry `setupTelemetry` OTLP。
- **阶段 44 角色装配真正拆分**（此前 `-role gateway/admin` 语义倒挂：gateway 不启
  IM、admin 缺管理面之外的会话/对话 API）：`cmd/trpc-service/role.go` 的
  `planFor` 给出能力矩阵（AdminAPI / ChatAPI / WorkerLoop / IMGatway），
  `registerAdminAPI` 只装管理面，`startDataPlane` 按 plan 分别启 worker 回路与 IM
  网关，未知角色 `exit 2`。**运行时探针**（4 节点 + 真实 MySQL/Redis，同 token）：
  `/tenants` 仅 all+admin 200；`/sessions`、`/dlq`、`/chat/stream` 仅 all+worker；
  gateway 仅 `/healthz`。证据：`role_test.go` + 实机探针。
- **生产门禁（阶段 44）**：`server.production=true` 时缺 `TRPC_JWT_SECRET`（或
  credential master key）**拒绝启动**（实测 exit=1），缺 `ADMIN_PASSWORD` 时跳过
  内置默认口令引导；`ReadHeaderTimeout` 默认 15s 防慢头攻击。
- 协作链路：adapter→`PublishInbound`(bus)→worker `ConsumeInbound`→`runner.Run`；
  回复走 MySQL Outbox→`stream:outbound`→gateway `ReadOutbound`→adapter.Send。

### A3 多节点水平扩展 + 消息路由到正确租户/session 【✅】
- `infra/bus/bus.go` inbound stream + `ConsumeInbound`（消费组）；worker 加入 group "workers"、
  consumer 名带 PID（多节点并存）；`RouteKey=route:{tenant}:{session}`（bus.go），
  消息体含 TenantID/SessionID，worker 每消息取上下文（worker.go）。

### A4 无 sticky session、依赖共享 Session/Memory 后端 【✅】
- 无本地会话状态；worker 经 `Router.Sessions(ctx, m.TenantID)` 取共享后端；
  会话跨节点串行由 Redis Lua 会话锁保证（bus.go）。

### A5 租户隔离：配置/数据/工具权限/日志脱敏/密钥管理 【✅】
- 配置隔离：各 MySQL store 查询带 `tenant_id`；数据隔离：session/memory 以
  `AppName=tenantID` 为框架边界，Redis key 带租户前缀。
- 工具权限隔离：`domain/tool` + Registry.IsAllowed + 租户白名单（policy.go）。
  **阶段 44 补强**：`agent_tool_grants` 增加 `tenant_id` 列（DDL 004 + 内存/MySQL 双
  store + 写入方），运行期 RBAC 改用 `IsAllowedForTenant` —— 授权行若属于别的租户
  则不生效（空租户=旧行，保持兼容）；API 侧 grant/revoke 仍先做同租户校验（跨租户
  404）。证据：`tool_test.go::TestGrantIsScopedToItsTenant`、
  `tool_handler_test.go::TestToolGrantRejectsForeignTenantAgent`、
  `toolstore/tool_mysql_integration_test.go`（真实 MySQL PASS）。
- 日志脱敏：`domain/governance` RedactionFilter 经 worker 挂载；
  **新增**统一日志脱敏：`infra/log` 对敏感 attr（password/secret/token/dsn/api_key/…）
  自动打码（log.go redactHandler + `log.Mask`）；**阶段 44 补强**：`slog.Any`
  传入的字符串/error 同样脱敏，且任意字符串值里的 URL 凭据（`scheme://user:pw@`、
  `?token=`/`&secret=`）经 `SanitizeText` 清洗，敏感键补 `access_key/secret_key/
  private_key/signing_key`。证据：`log_redact_test.go::TestSanitizeTextStripsURLSecrets`
  / `TestRedactsNonStringValues`。
- 密钥管理：`infra/secret` AES-256-GCM，master key env 注入。
- **资产级隔离（阶段 40 补齐）**：`app/web/permission.go` 提供资源级原语
  `ClaimTenant`（写入锚定调用者租户）/ `TenantAccessible` / `EndpointAccessible`
  / `WriteCrossTenant`（越权统一 404，不确认外部资源存在）；`ScopeTenant`
  非 owner 省略 `?tenant_id=` 亦钉死本人租户。Agent/端点/KB(含文档·检索)/
  Skill(含版本)/工具授权/通道绑定/租户自身的**按 id 路由**全部校验归属，
  create 不再信任请求体 tenant_id；worker `run()` 校验
  `agent.TenantID == message.TenantID`。证据：`tenant_isolation_test.go`、
  `kb/skill/channel/tool/tenant_handler_test.go`、`stores/tenant.spec.ts`。
- **资产作者制与显式共享（阶段 41 补齐）**：租户资产（KB/Skill/IM 绑定/端点）
  加 `created_by` + `visibility`（默认 private），作者可改/删并主动共享；
  shared 对同租户他人**只读**，private 对他人 404；admin/owner 管本租户全部；
  global 资产（Skill/端点）仅 owner 可建改。member 成为资产生产者：可创建并
  共享四类资产、看工具目录并给自己的 Agent 授权工具、查看**自己触发的**用量
  （`usage_records.member_id` + `ScopeMember` 强制钉住）；仍不可管理租户/成员/
  密钥/审计/DLQ。**每次资产写操作落 audit_logs**（`decision=executed|deny`，
  被拒探测同样入账）。证据：`asset_sharing_test.go`（4 用例）、
  `usage_handler_integration_test.go::TestUsageAPIMemberSeesOwnUsageOnly`
  （真实 MySQL PASS）、`rbac_matrix_test.go`、DDL `014_asset_ownership.sql`。

### A6 不同租户可选不同数据后端 【✅ 设计定案】
- `infra/storage/router.go`：session/memory 按 `Tenant.DataBackend` 逐租户路由
  （inmemory/mysql/redis）；其余域全局单后端（knowledge=Milvus、artifact=MinIO、
  audit=MySQL）。
- 收敛准则「出现第二实现才纳入 Router」与边界已文档定案：
  `tempdocs/租户配置模型与数据后端路由设计.md` → ✅。

### A7 统一数据访问抽象：Session/Memory/Summary/Artifact/Knowledge/Audit 如何存储 【✅】
- `infra/storage/router.go` + `router_domains.go`：session/memory 逐租户路由，
  vector/artifact/audit 为 Router 包装的 dispatcher；Summary 无独立域（随框架
  session 后端，`tenants.data_backend` 无 `summary` 键）。权威说明
  `tempdocs/存储与数据访问设计.md`。阶段 44 删除 `stores.go` 的 `DataStores` 聚合
  （构造后立即拆包的中间态），main 直接构造并注入。
- ✅ **Memory 真实接线（阶段 44，此前只建了抽象未接线）**：worker 每轮经
  `Router.Memories(tenantID)` 取后端 → `runner.WithMemoryService` + 挂
  `memory.Service.Tools()`（工具驱动显式写入，**不配 extractor**）+ 构建 Agent 时
  `llmagent.WithPreloadMemory(memory.preload_count)`（0 关闭，默认 10）。证据：
  `worker/memory_test.go`（关闭时不挂工具/不注入；跨会话记忆写入后被预加载进第二轮
  prompt；关闭时不泄漏；租户不可见）；**compose 实机**：真实模型下 `memory_add`
  写入 `mem:{t-demo:u-mem-3}`（审计 tool_name=memory_add），同用户新会话未调用
  检索工具即答出「你平时最喜欢喝美式咖啡。」。

### A8 数据同步策略 + 跨后端迁移 【✅ 实现 + 边界如实标注】
- ✅ 同 session 并发一致：Redis Lua 会话锁 + 续期（bus.go）。
- ✅ IM 重复投递幂等：Redis SetNX + outbox idempotency_keys 唯一键 + adapter 去重。
  **阶段 44 改为两阶段**：`Idempotent` 只取**租约**（90s，心跳续期），回复落 outbox
  后才 `CommitIdem`（24h），失败/崩溃由 `ClearIdem` 或租约到期释放 → 不再出现「崩溃
  后 ack 掉、用户永远收不到回复」。证据：`TestBusIdempotencyIsATwoPhaseLease`、
  `TestWorkerCommitsIdempotencyAfterTheReplyIsDurable`、
  `TestWorkerReleasesTheLeaseOnAFailedTurn`；compose 实机旁证（失败轮次后
  `idempotency_keys` 无记录）。
- ✅ memory 写后跨节点可见性：共享 Redis/inmemory 后端（memories.go）；MySQL memory
  后端不存在 → 无「异步落 SQL」分支（按实现收敛，README 未声明）。
- ✅ **跨后端迁移程序（上版 ❌ 已实现）**：`cmd/migrate` CLI（session state+events、
  memory 全量，delete-then-create 幂等；`-auto` 枚举 MySQL 源用户；roundtrip 单测
  `cmd/migrate/migrate_test.go`）。操作手册：`tempdocs/跨后端数据迁移操作手册.md`。
- ✅ **事件级幂等（阶段 44 补齐）**：轮次把入站消息 id 作为 `RunOptions.RequestID`，
  框架据此给该轮所有事件（含中断的助手输出）打标；重放时 `worker.resumeRecordedTurn`
  从会话事件日志恢复「已产出但未落 outbox」的回复 → 不重复调用模型、会话中不出现
  第二条用户消息；部分流式回答不恢复（宁可重跑，不交付截断内容）。证据：
  `event_idempotency_test.go` 三用例（重放后用户轮次=1、模型调用=1、回复取自首次记录；
  部分流式被忽略；他人 RequestID 不误用）。
- ⚠️ 已如实标注的边界（非缺口）：框架 v1.11.2 无事件分页公开 API → events 迁移为
  后端「最近事件窗口」；summaries/tracks 无 Service 写入 API → 不迁、目标端重建；
  MySQL 全量补拷 SQL 模板见手册 §5。→ 综合判 ✅（迁移能力落地，边界文档化）。

---

## B 组：IM 接入

### B1 至少两类 IM Channel Adapter（含企业微信） 【✅】
- Adapter/Conn 契约 channels.go:33-61；企微真实 SDK 长连接（wecom/conn.go:78-81）、
  飞书 lark ws.Client（feishu/conn.go）；gateway.go 装配；两 SDK 入 go.mod。

### B2 外部 IM 消息 → model.Message / Runner；Event → IM 回复 【✅ 文本 + 附件 + 卡片链路全通】
- gateway.go `buildUserContent` → bus.Message；worker.go `r.Run`。
- 企微流式占位/原位替换已实现（wecom/conn.go）；飞书整段文本。
- **附件（阶段 45）**：适配器 `fetchMedia` 取回字节（wecom 下载+AES 解密 / feishu message-resource，
  8 MiB + 15s 上限，失败只记 `FetchError` 不丢消息）→ `buildUserContent` 变成 `ContentParts`
  （图片 `AddImageData`、文件 `AddFileData`）+ 消息文本里的「附件清单」→ 框架
  `session/externalization` 把内联字节落 MinIO 制品（事件里只留 `artifact://name@ver`）。
  证据：`channels/media_test.go`（6 例）+ `worker/media_test.go`
  `TestExternalizeSessionContentArchivesInlineAttachments`（真实归档 + 引用 + 回填）。
- **卡片（阶段 45）**：审批通知在飞书渲染为交互卡片，按钮回调经 `CardActionToInbound`
  复用「回复批准/拒绝」同一条链路。证据：`worker/approval_test.go`
  `TestApprovalNoticeIsACardOnButtonCapableChannels`、`feishu/card_test.go`、
  `feishu/webhook_test.go::TestNormalizeWebhookCallbackCardAction`，以及
  `bus` 的 `TestMessageRoundTripPreservesCardAndMedia`（修复 Kind/Segments 被 Stream 丢弃）。
- 逐字流式仍无生产者（企微=占位+终稿，飞书无原生流式）→ 如实标注。

### B3 账号/租户绑定、验签、去重、身份映射 【✅】
- ChannelBinding（credential_ref/verification_token_ref 均密钥引用）+ ChannelAPI 入库前写 secret；
  wecom SHA1+AES / feishu SHA256 验签；platformMsgID 去重 + adapter 内存去重 + worker 双幂等；
  open_id/user_id → UserID 身份映射。
- **HTTP 回调入口（阶段 45）**：`POST /webhooks/im/{binding_id}`（默认关，`im.webhook`
  配置开启），签名为 faishu `sha256(timestamp+nonce+encrypt_key+body)`（复用 SDK
  `event.Signature`），支持加密体（`event.EventDecrypt`）与 `url_verification` 握手；
  fail-closed（无密钥/解析失败/签名不符 → 401），企业微信无回调模式 → 启动即 `exit 2`。
  证据：`channels/webhook_test.go`（11 例）、`feishu/webhook_test.go`（8 例）、
  `web/webhook_test.go`（5 例）、`cmd/trpc-service/gateway_test.go`、
  `web/auth_rbac_test.go::TestAuthMiddlewareSkipsPathPrefix`。

### B4 session_id 规则 + 群/单聊 + 跨租户隔离 【✅】
- `BuildSessionID`：单聊 `tenant:channel:user:id`、群聊 `tenant:channel:group:id`；
  内嵌 tenant + route 键隔离。

### B5 IM 平台限制（长度/频率/异步/图片文件/撤回/失败重试） 【✅ 实现 + 策略文档】
- ✅ 出站截断：`channels/limit.go` `maxTextLen`（wecom 2000/feishu 4000/兜底 4000）
  + `truncateText`（rune 安全 + `…`），gateway.Run 发送前执行（gateway.go）。
- ✅ 入站频率限流：`RateLimiter` 契约 + `RedisRateLimiter`（INCR+EXPIRE 固定窗口，
  多节点共享）+ gateway.pumpInbound 拦截 + config `rate_limit{enable,per_minute}`
  + main 装配（默认关）；单测 channels/limit_test.go。
- ✅ 异步回复 / 幂等重试：同 B2/B3/A8。
- ✅ 媒体（阶段 45 由"忽略"改为"真正处理"）：下载 → 内联给模型 → 落 MinIO 制品 →
  失败明确回执 + 审计（`error_type=attachment_unreadable`）；策略文档
  `tempdocs/IM平台限制与降级策略.md` §4/§5。
- 撤回事件：识别已存在、行为=忽略（无消息回收需求），扩展接缝说明同文档。

### B6 群聊 @ 门禁与真实收发完成度【✅ 交付完成；真实收发已实测通过（2026-09）】
- 飞书群 @ 门禁（feishu/feishu.go:102,133）；企微群全处理。
- mock 冒烟（可自动跑，不依赖真实 IM）：`go test ./trpcservice/infra/channels/`
  （gateway/manager/limit 测试覆盖 入站→bus→outbound→Send、路由、去重、限流、截断）。
- 真实企微/飞书收发需真实凭据：配置步骤 + 验证清单见 `tempdocs/IM联调手册.md`；
  代码已就绪，判定为外部凭据等待项而非代码缺口 → ✅(交付) + ⚠️(联调)。

---

## C 组：治理、监控与安全

### C1 租户级治理（Plugin/Guardrail/Callbacks）【✅】
- 工具白名单（policy.go + worker.go）、脱敏 RedactionFilter（governance + worker.go）、
  预算 budgetExceeded（软限、失败放行，注释明示）、危险工具三轨审批（approval）、
  IM 用户权限 imUserAllowed（仅 IM 通道）。全部有单测覆盖（policy/approval/worker 测试）。

### C2 监控指标 【✅ 全指标埋点打通】
- 埋点（全部生产调用点）：`inbound_messages`(worker.go)、`outbound_messages` 与
  `im_delivery_total{ok}`(channels/gateway.go send 后)、`agent_run_duration`(worker.go)、
  **`model_call_duration`（阶段 33 新增**：worker `model_timer.go` plugin Before/AfterModel
  累计单回合模型耗时，worker.go 记录）、`tool_call_duration`、`token_usage` 与
  **`tenant_cost`（阶段 33 接通**：worker.go 回合末 TokenUsage 同点记录）、
  `session_latency`（worker.go Sessions 调用计时）、`agent_run_errors`。
- 成本口径：token 数（无价格表，换算留扩展）→ 已文档化。观测用法：`tempdocs/容量评估指南.md` §3。

### C3 OpenTelemetry trace 贯穿 IM→Runner→Tool→Session/Memory→回复 【✅】
- Span：`im.callback`(gateway)、`im.reply`(gateway)、`agent.run`(worker)；
  TraceID 随 bus.Message 传播；setupTelemetry 接线。
- **Session/Memory 有独立子 span（阶段 45 补齐，此前只有"自动合并到当前 span"的说法）**：
  `infra/storage/tracing_sessions.go` / `tracing_memories.go` 逐方法开
  `session.*` / `memory.*` span（属性 app_name/user_id/id、event.request_id、
  memory.count、storage.domain/backend，失败 `RecordError`+`codes.Error`）。
  证据：`storage/trace_test.go`（5 例）+ `worker/trace_chain_test.go`
  `TestTurnTraceCoversSessionAndMemory`（**真实回合**断言 parent == `agent.run`）。

### C4 审计日志字段（11 项全齐）【✅】
- `infra/audit/audit.go:29-41` Entry 11 字段一一对应；worker.recordAudit 全填充
  （Cost=tokens、ToolName 逗号拼接）。审批决定写 approval。

### C5 密钥管理与脱敏【✅ 核心 + 阶段 33 补齐日志/DSN】
- ✅ 模型 APIKey 经 APIKeyRef→KeySource 不留明文；IM token 经 secret store；
  AES-256-GCM + master key 不落库（secret/mysql.go）；工具入参/结果脱敏。
- ✅ **缺口1 日志统一脱敏（上版 ⚠️ 已实现）**：`infra/log` 默认 handler 对敏感 attr
  （key 含 password/secret/token/dsn/api_key/accesskey/authorization/credential 等）
  自动 `Mask`（含嵌套 group），worker/slog 全链路生效（main 使用 `srvlog.New`）。
- ✅ **缺口2 audit 错误脱敏（上版 ⚠️ 已实现）**：`recordAudit.ErrorType` 改为
  `classifyRunError` 分类（""/timeout/run_error，worker.go），裸错误串不再入库。
- ✅ **缺口3 DSN 明文（上版 ⚠️ 已实现）**：config.Load 支持 `${ENV}` 展开
  （config.go + 单测）；`deployments/backend-compose.config.yaml` dsn 改为
  `${TRPC_MYSQL_DSN}`；compose backend environment + `.env.example` 注入
  TRPC_MYSQL_DSN → DSN 不进版本库明文。

---

## D 组：故障恢复与运维

### D1 降级策略【✅】
- 节点故障重投（XAUTOCLAIM）、DB 不可用回落内存（main.go）、模型超时
  （HTTP 120s + runTimeout 300s + WithTimeout）、工具失败语义（非零退出→Output）。
- **阶段 44 补齐的降级面**：①启动**等待依赖**（`deps.go`：MySQL/Redis 各 60s 轮询，
  超时才进降级模式）——此前节点先于数据库启动就永久降级；②Outbox 派发遇瞬态错误
  **不再退出循环**（记录并重试，退避 + 重试上限 + `dead` 终态与 `last_error`）；
  ③轮次失败原因落 WARN 日志（此前审计只记 coarse `run_error`，日志里查不到原因）；
  ④IM 适配器断线**退避重连**（不再静默变聋）+ `Manager.Close` 随 runCtx 释放连接；
  ⑤记忆后端不可用该轮降级为「无长期记忆」并告警，不影响回复。

### D2 context/goroutine/Runner 排空【✅ 阶段 33 补优雅关停】
- worker 整轮 WithTimeout；bus worker 池 + ctx.Done；runner defer Close + 事件排空；
  审批轮询退出；审计批量 Close。
- ✅ **顶层优雅关停（上版 ⚠️ 已实现）**：`main.go` 用 `signal.NotifyContext`
  （SIGINT/SIGTERM）驱动：worker/outbox/IM gateway 的 Run 改接 runCtx；
  HTTP server `Shutdown` 5s drain；defer 触发 auditRec.Close 与 telemetry cleanup。

### D3 灰度发布与租户级配置回滚【✅ 阶段 44 补齐真实灰度分流】
- ✅ Agent 不可变版本 + 原子回滚（agent.go Publish/Rollback + agent_versions +
  POST /agents/{id}/rollback）。
- ✅ **租户配置版本历史 + 回滚**：`tenant_config_versions`
  （deployments/mysql/init/011_*.sql）+ tenant.Store Snapshot/List/Get +
  Manager.Update 自动快照 + `RollbackConfig`；web
  `GET /tenants/{id}/config-versions` + `POST /tenants/{id}/config-rollback`；
  单测 config_version_test.go / tenant_handler_test.go。
- ✅ **版本级灰度分流（阶段 44 实现，此前只有回滚）**：`agents.gray {version,percent}`
  （003_agents.sql）+ `agent.Manager.ResolveForSession` 按**会话 id 稳定分桶**
  （`GrayBucket` = fnv32(sessionID)%100 < percent），同一会话全程固定版本；
  `PUT /agents/{id}/gray`（非法目标 400：未发布版本 / 等于当前版本 / percent 越界）
  与 `DELETE /agents/{id}/gray`（即时回滚，无需重新发布）；worker 在 `run()` 取
  profile 时应用并把实际版本写入 `agent.run` span 的 `agent_version`；前端 Agent 页
  「灰度 / 清除灰度」+ 灰度列。测试：`gray_test.go`（分流比例与稳定性 / 清除回滚 /
  非法目标 / 版本消失回退）、`TestAgentGrayReleaseIsTenantScopedAndValidated`、
  `agentstore` MySQL 集成测试；**compose 实机**：设置 30%→v1 回读一致、v9 → 400、
  清除后 gray=null。
- 灰度与配置回滚策略文档：`tempdocs/灰度发布与配置回滚策略.md`（阶段 44 后与代码一致）。

### D4 容量评估【✅ 方法论 + 真实模型实测数值已回填】
- 方法论 + 分域公式 + 观测指标表 + 压测步骤 + 治理护栏：`tempdocs/容量评估指南.md`
  （基线容量表 `docs/详细设计文档.md` §14）。
- 代码支撑（运行上限/观测埋点）：`consumeWorkers`(bus.go)、`runTimeout`(worker.go)、
  token 预算、IM 限流、C2 全指标。
- ✅ **实测数值已回填（2026-09-09，真实模型栈）**：`tempdocs/容量评估指南.md` §4.1 ——
  入队侧 p50≈3~5ms 非瓶颈；端到端回合 p50≈4~5s（与并发无关）；~10~15% 回合拖尾
  至 ~300s（上游模型抖动，护栏内 0 失败）；单节点 16 槽保守吞吐 ≈0.33 回合/s。

### D5 最小运行与生产部署【✅】
- compose 全栈（mysql/redis/etcd/minio/milvus/otel/jaeger/prometheus/frontend）；
  backend-compose.config.yaml + otel-collector.yaml；init 001~013.sql（13 个，
  **init 目录即全量 schema**；阶段 43/44 的加列改动直接写进建表语句，既有数据卷的
  手工 ALTER 语句写在各文件头部注释）；begin.sh 一键（含 `reset` 重建数据卷）；
  K8s 清单已移除（阶段 25 决策）。
- ✅ **分层启动（阶段 44 加 profiles）**：核心四件（mysql/redis/backend/frontend）无
  profile；可选服务分三组并同时挂 `full`——`vector`（etcd+minio+milvus）、
  `artifacts`（artifact-minio）、`observability`（otel-collector+jaeger+prometheus）；
  `.env.example` 默认 `COMPOSE_PROFILES=full`（等价旧行为）；`./begin.sh core` 只起
  核心四件。可选三组移出 backend `depends_on`（缺失即文档化降级：KB 检索不可用/
  制品关闭/无 trace 导出），否则核心模式无法启动。实测 `docker compose config
  --services`：默认 11 个、`COMPOSE_PROFILES=` 4 个、`COMPOSE_PROFILES=vector` 7 个。
- **阶段 44 实机复核**：`docker compose up -d --build backend frontend` 后
  `mysql ready`/`redis ready` → worker + IM 网关启动 → 灰度 API 与真实模型下的
  记忆写入/预加载均通过（详见 `tempdocs/代码级全面复核报告.md` §13.6）。

---

## 汇总（阶段 45 修订）

| 结论 | 条目 |
| --- | --- |
| ✅ 完整实现（设计定案/文档化） | A1 A2 A3 A4 A5 A6 A7 A8 B1 B2 B3 B4 B5 C1 C2 C3 C4 C5 D1 D2 D3 D4 D5 |
| ⚠️ 外部依赖等待项（非代码缺口） | ① D5：上 K8s 后的 HPA（观测指标已就绪）。② B2/B3/B5 的**真实平台行为**（卡片按钮点击、附件下载、HTTP 回调验签）需真实 IM 账号手测——代码、配置、单测都已就位，见 `tempdocs/IM联调手册.md`。B6 真实企微/飞书文本收发与 D4 压测数值已完成并回填 |

**阶段 45 补齐清单（上版 → 本版）**
- B2：入站附件从"识别但忽略"改为**下载 → ContentParts 内联给模型 → MinIO 制品归档 → 读取失败明确回执 + 审计**；审批通知在飞书从纯文本改为**交互卡片 + 按钮回调**（同一审批链路）；顺带修复 `bus.encode/decode` 丢弃 `Kind`/`Segments` 导致卡片静默降级为文本的真实缺陷。
- B3：新增 **IM HTTP 回调入口** `POST /webhooks/im/{binding_id}`（默认关闭、fail-closed 验签、飞书事件订阅模式；企业微信无此模式 → 配置即拒绝启动）。
- C3：Session/Memory 从"自动并入当前 span"改为**真正逐方法开子 span**，并有真实回合的父子关系断言。
- 文档：`docs/架构设计文档.md` 新增 §10.5（框架 `server/*` 未接线的取舍）与 §10.6（租户模型配置映射，说明为何不恢复 `model_config` 列）；`docs/风险清单.md` R10 改写为真实实现。

**阶段 33 补齐清单（上版 → 本版）**
- A8：`cmd/migrate` 跨后端迁移 CLI + 手册（上版 ❌）；
- B5：出站截断 + Redis 入站限流 + 策略文档（上版 ❌/⚠️）；
- C2：outbound/im_delivery/tenant_cost/session_latency 生产埋点 + 新增 model_call_duration（上版 ❌ 4 指标未埋点）；
- C5：slog 统一脱敏 + audit ErrorType 分类 + DSN 走 env（上版 ⚠️×2）；
- D2：signal 驱动优雅关停（上版 ⚠️）；
- D3：租户配置版本表 + 回滚 API + 灰度策略文档（上版 ❌）；
- D4：容量评估指南（上版仅一段容量表）；
- A1/A6：设计取舍文档定案（上版 ⚠️）。
- B6：联调手册 + mock 冒烟定义（上版待手测）。
