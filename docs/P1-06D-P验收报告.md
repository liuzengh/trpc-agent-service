# P1-06D-P 验收报告：production Memory repository / projection enqueue / worker and retrieval composition

## 结论（当前权威状态）

- `P1-06D injected retrieval/hydration boundary: PASS`（保持）
- `Production PostgreSQL Memory repository: PASS`
- `Transactional Memory + vector task enqueue: PASS`
- `Production SourceProjector: PASS`
- `Production vector worker composition: PASS（default disabled；enabled 且配置完整时可构造并运行）`
- `Production retrieval composition: PASS（default disabled；同上）`
- `Enabled runtime without production Embedder: FAIL CLOSED（真实二进制验证）`
- `Production Embedder: NOT IMPLEMENTED`
- `Production request path exposure: NOT IMPLEMENTED（未新增任何公共 API，未接入 /api/chat）`
- `P1-06D: verified only as a production-composable Memory retrieval/hydration boundary`
- `P1-06E: blocked/not started`
- `P1-06: partial`

## 交付范围（全部新文件，无 migration/dependency/config 提交）

- `trpcservice/vector/task/postgres.go`：重构出 `queryExecutor`（pool/tx 共享同一 SQL 与校验），新增 `EnqueueTx`（caller-owned 事务内 enqueue；同一 dedup key、同一状态默认值、同一超时；不获取 lease、不触碰 Redis/后端）。原 `Enqueue` 行为不变。
- `trpcservice/vector/task/source_postgres.go`：production `PostgresSourceProjector`（allowlist 仅 memory；tenant-scoped；校验 scope/version/sequence/deleted/content-hash；upsert 遇 tombstone 与 delete 遇 active 均为安全 stale 分类；delete 边界不携带 content；不读 `vector_ref`；错误仅类别）。
- `trpcservice/storage/postgres/memory.go`：production `MemoryRepository`（实现既有 `storage.MemoryRepository`）：`Put`（行锁下 server-owned version/seq 单调推进 + 同事务 `EnqueueTx` upsert task）、`Delete`（tombstone + 同事务 delete task；重复 delete 幂等不新建任务；missing→ErrNotFound）、`Get`、bounded `Search`（deleted 不返回、limit 1..100）；caller 不能指定 tenant/document/task/projection identity；0-row/unknown 不视为成功；raw SQL/DSN 不外泄；依赖窄接口 `projectionTaskEnqueuer`（组合层适配，避免 task 内部测试导入环）。
- `cmd/trpc-service/vector_composition.go`：`vectorComposition` + `assembleVectorComposition`——默认全禁（VECTOR_BACKEND 未设/none 且 worker/retrieval 未启用时返回 nil，不构造任何 client/credential/goroutine）；enabled 要求 backend=milvus + endpoint/collection/model/version/schema/dimension/redis 完整且 `VectorEmbedder` 非空（production seam，默认 nil），任一缺失 fail closed；部分构造失败自清理；`milvusEndpointResolver`/`milvusCredentialResolver` 走既有 env/SecretRef 边界。
- `cmd/trpc-service/production.go`（最小接线）：`productionRuntime.vector`；Start 顺序 = queue worker → vector（store Ready bounded → worker start）→ dispatcher hook → setStarted；Stop 顺序 = vector worker drain → queue worker → dispatcher → vector store/redis close → pool；ForceClose 追加 vector worker bounded stop；readiness 增加 `vectorProbe`（仅 enabled 时非 nil；disabled 行为不变）。
- 测试：`trpcservice/storage/postgres/memory_integration_test.go`（真实 PG：原子提交、回滚一致、重复收敛、version/seq、tombstone/delete task、tenant A/B、bounded search、SourceProjector 全矩阵）、`cmd/trpc-service/vector_composition_test.go`（disabled 默认矩阵、enabled fail-closed 10 例、无 production Embedder fail-closed、seam 构造、runtime default-disabled）、`cmd/trpc-service/vector_milvus_e2e_test.go`（integration tag：真实 production-style 全链路 E2E）、`trpcservice/vector/milvus/testsupport_integration.go`（integration-tagged 测试支持导出，production 代码零引用）。

## 验证证据（GOFLAGS=-mod=readonly，全部远程实测）

- Memory repository/transactional enqueue/SourceProjector integration（真实 PG）：PASS（ordinary；race 由 full race 覆盖同一套测试）。
- 真实服务二进制：A 默认 disabled `/healthz=200` PASS；B1 worker-enabled 缺配置 exit 1 PASS；B2 完整配置但无 production Embedder exit 1 PASS；B3 retrieval-enabled 无 Embedder exit 1 PASS。
- local production-style E2E（真实 assembly + 真实 local Milvus v2.5.0 认证启用 + 真实 Redis lease + 真实 PG task/memory 表 + deterministic test embedder 仅经 seam 注入）：PASS——upsert→同事务任务→worker claim→SourceProjector→fenced success→retrieval/hydration 返回权威内容→update→v2→tombstone→delete→不可再检索。
- P1-06C regression：vector/task ordinary+race、task integration（真实 PG/Redis）、local Milvus worker E2E 均 PASS（`Enqueue` 行为未变）。
- P1-06D retrieval regression：retrieval unit/integration ordinary+race PASS。
- affected ordinary/race（vector/…、storage/…、cmd/trpc-service）PASS；P1-04 `TestProductionPostgresWebhookFailureWindows` 5/5 PASS。
- full ordinary：30 包 PASS；full race：30 包 PASS；DATA RACE 未观察。
- vet（ordinary + integration tags）PASS；gofmt clean；targeted diff-check（tracked docs）0 findings；本轮新增文件 whitespace 0。

## 边界声明

- Memory 事实与 vector task 在同一 PostgreSQL 事务原子提交（真实测试通过）；embedding/Milvus/Redis 调用一律在事务外。
- task 与 Milvus 不构成跨系统 exactly-once；stable document ID + fenced task 支持未来 reconciliation，本轮未实现 reconciliation。
- 默认 `VECTOR_BACKEND=none`、worker/retrieval disabled；fake Embedder 仅存在于测试 seam/integration fixture，不可能成为 production 默认。
- Production Embedder NOT IMPLEMENTED：真实二进制 enabled 无 Embedder 即 fail closed（B2/B3 证据）。
- Production Milvus NOT RUN；semantic retrieval quality NOT TESTED；cloud IAM/TLS/HA NOT RUN。
- 本轮外部请求：Telegram/Lark/Model Provider/Embedding Provider/Production Milvus 全部 `0`（DeepSeek 凭据未使用；二进制场景改用 synthetic placeholder）；local Milvus/PG/Redis 请求仅限本轮 owner-scoped localhost（`p106dp-milvus-*`/`p106dp-reg-*`）。
- 清理：全部 owner/run 资源 label 验证后删除（containers/networks/volumes=0）；临时文件删除；无 prune；无未知资源删除；security clean NOT CLAIMED（scanner UNAVAILABLE，历史 findings 保留）；无 commit/push。
