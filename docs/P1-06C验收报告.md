# P1-06C 验收报告：durable vector task / claim / reclaim / fencing / retry / DLQ / vector worker

## 结论（当前权威状态）

`P1-06C: verified only as the durable vector-task boundary (migration + repository + Redis lease + worker + local Milvus E2E)`。

- P1-06A 维持 `verified (contract/projection/fake-embedding boundary)`。
- P1-06B 维持 `verified only as a local Milvus adapter/integration boundary`。
- P1-06C 本轮在真实 PostgreSQL/Redis/local Milvus 上完成 durable task 生命周期验证。
- P1-06D（retrieval/hydration/fallback service）保持 `blocked`，未实现。
- P1-06E（rebuild/reconciliation/resume/version migration）保持 `blocked`，未实现。
- `P1-06: partial`。production vector worker 尚未接入 production composition root；production backend 仍为 `vector=none`。

## 交付范围（本轮实际改动）

新增（全部为 untracked 新文件，未触碰既有公共 contract）：

- `migrations/000007_vector_projection_task.up.sql` / `.down.sql`：`vector_projection_task` 表，状态机 CHECK（pending/running/retry_wait/succeeded/dead_letter/stale/cancelled）、attempt<=max_attempts、running 必须携带完整 server-owned lease 身份、task_id `^vt-v1-[0-9a-f]{32}$`、document_id `^vd-v1-[0-9a-f]{64}$`、content_hash 64-hex、安全错误类别 allowlist；ready/lease-expiry 部分索引、document head 索引、created 索引；tenant 外键 CASCADE。
- `trpcservice/vector/task/model.go`：Task（仅 server-owned 身份/投影标记/协调状态，无 source content/embedding/凭据）、server-derived `TaskID`（tenant+document+operation+version+sequence，内容 hash 不参与身份）、LeaseRef、HeadKey（version→sequence→delete-dominance）、bounded `Config`、`classifyVectorError`（含 `context.Canceled`→release、`DeadlineExceeded`→retry）。
- `trpcservice/vector/task/repository.go`：Repository 接口（Enqueue/Get/Candidate/Claim/ExtendLease/Complete/Fail/Cancel/Head/Counts）。
- `trpcservice/vector/task/postgres.go`：`PostgresRepository`。全部变更为 tenant-scoped 条件更新并检查 RowsAffected：Claim 条件含 durable attempt 预算与到期 reclaim（attempt>=max 的到期候选先由同语句 CTE 归档 dead_letter）；Complete/ExtendLease/Fail 以 owner+epoch+fence+未过期租约为 CAS 条件；Complete 内嵌 NOT EXISTS document-head guard（严格更新的 succeeded head 使完成转为 stale 分类路径）；Fail 支持 retry/release/stale/dead_letter，release 归还 pending 并回退本次未执行 attempt；unknown provider 结果不得记为 success；错误仅暴露类别（ErrNotFound/ErrNotClaimable/ErrConflictOwnership/ErrUnavailable），不泄漏 SQL/DSN。
- `trpcservice/vector/task/source.go`：注入式 `SourceProjector` seam（production Memory/Knowledge durable source 未实现）与 `verifyLoadedSource`（projection 身份漂移→invalid_request DLQ；版本/顺序/内容漂移→stale）。
- `trpcservice/vector/task/worker.go`：`Worker`：poll→Redis lease Acquire→durable Claim（退出 claim 后才进行外部调用）→Load source→embed→`VectorStore.Upsert/Delete`（provider-neutral，绝不在 PostgreSQL 事务内）→fenced Complete/Fail→lease Release；lease renewal 失败立即取消执行路径；任务级超时；Stop/Wait/StartContext/Run bounded drain；取消/关闭分类为 release，不烧掉 attempt，不误报 provider 失败。
- `trpcservice/vector/task/task_test.go`（unit ordinary+race）、`trpcservice/vector/task/integration_test.go`（真实 PostgreSQL+Redis，owner-scoped）、`trpcservice/vector/milvus/worker_integration_test.go`（真实 local Milvus worker E2E）。

受影响既有测试的机械性更新（version 7 引入所需）：

- `trpcservice/storage/postgres/postgres_integration_test.go` 的 `TestPostgreSQLMigrations`：checksumSource/failedSource 文件清单补入 `000007_vector_projection_task` 真实文件对；apply-failure probe 由 `000007_broken` 顺延为 `000008_broken`，failed-version 断言相应改为 version 8。未改动任何 migration/production 代码。

未修改：P0-09F、Queue、Worker、Completion、Outbox、Dispatcher、既有 Redis epoch/fencing 核心算法、P1-04、P1-05、P1-06A 公共 contract、`/api/chat`、`go.mod`、`go.sum`（go.mod/go.sum mtime 均早于本轮全部写入）。

## 验证证据（全部远程实测）

1. Task unit ordinary+race：PASS（含 TaskID 稳定性与内容无关、head ordering delete-dominance、分类表、backoff 上限、cancel→release、newer-head conflict→stale、source 校验、Ref 还原、依赖 fail-closed、unknown→retry、预算→DLQ、complete+stop lifecycle）。
2. 真实 PostgreSQL/Redis integration（owner-scoped DockerLab 或 `TEST_DATABASE_URL`/`TEST_REDIS_URL`）：enqueue 幂等/去重收敛、Claim/Complete、Head 读取、过期 owner 完成被拒绝且新 owner 可 reclaim、retry 预算耗尽→dead_letter 且不再可 claim、Counts —— PASS。
3. 真实 local Milvus worker E2E（standalone v2.5.0，认证启用，owner-scoped，仅 127.0.0.1）：upsert 任务 succeeded 后 search 命中 server-owned document identity；tombstone delete 任务 succeeded 后 search 不再返回；Counts=2 succeeded —— PASS。
4. 真实服务二进制启动链：`TestCommandFailsClosedOnMigrationMismatch` 全部 5 子测试 PASS（normal migration 经真实 binary 顺序执行 migration 1–7 后 ready；checksum mismatch/unknown future version/missing version 保持 fail-closed 且输出 redacted；apply-failure probe 无残留表且未记录为 applied）。
5. 全量 ordinary 回归（28 包，真实 prerequisites）：PASS。全量 race 回归（28 包）：PASS，无 `DATA RACE`。`go vet ./...` 与 `go vet -tags integration ./trpcservice/vector/...`：PASS。`gofmt`：clean。被编辑 tracked 文件 `git diff --check`：0 新增 findings。

### 外部请求计数

- Telegram requests：`0`（历史 phase 的 >0 记录保留；本轮无任何 Telegram 请求，BOOTSTRAP 渠道均 disabled）。
- Lark requests：`0`。
- Model Provider requests：`0`（本轮用户提供 DeepSeek 凭据仅用于满足服务启动 runner 环境非空校验；只做启动+`/healthz`，未触发任何 chat/模型 API 请求）。
- Embedding provider requests：`0`（全部使用 deterministic local embedder）。
- Production Milvus requests：`0`（仅本轮 owner-scoped 本地 standalone）。

### 资源与清理

本轮创建的全部 Docker 资源（Milvus/etcd/MinIO standalone、回归 PG/Redis、各 DockerLab）均按 owner label 验证后删除：containers/networks/volumes remaining=`0`；无 prune、未触碰未知资源。测试内 credential/临时文件删除。

## 边界与未完成（NOT IMPLEMENTED / NOT RUN）

- production Memory/Knowledge durable source projector：NOT IMPLEMENTED（仅注入 seam）。
- production enqueue producer（Memory/Summary 写路径产出任务）：NOT IMPLEMENTED。
- production vector worker 接线（production.go composition、vector backend enable）：NOT DONE；production 仍 `vector=none`。
- retrieval/hydration/fallback（P1-06D）：BLOCKED，未实现。
- rebuild/reconciliation/resume/version migration（P1-06E）：BLOCKED，未实现。
- Production Milvus/Cloud IAM/TLS/HA：NOT RUN。provider-side exactly-once：NOT PROVEN（delete 重复执行仍返回 unknown 语义，继承 P1-06B）。
- security clean：NOT CLAIMED；历史 govulncheck findings 保留。

无 commit/push。
