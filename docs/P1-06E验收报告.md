# P1-06E 验收报告：durable rebuild / reconciliation / projection migration boundary

## 结论（当前权威状态）

- `P1-06E: verified only as a durable rebuild/reconciliation boundary`
- `projection migration boundary: pre-provisioned target rebuild PASS；cutover BLOCKED（无原子 alias 协议，未伪造切换）`
- `P1-06: verified only as a disabled-by-default, production-composable derived-vector lifecycle boundary`
- `production rollout: BLOCKED（Production Embedder NOT IMPLEMENTED；production request path NOT IMPLEMENTED；Production Milvus NOT RUN）`
- `P1-06E reconciliation scheduler/自动触发: NOT IMPLEMENTED（不新增公共/admin API，生产默认 disabled）`

## 交付范围（新文件；无 dependency 变更；go.mod/go.sum 哈希与接手一致）

- `migrations/000008_vector_rebuild_run.{up,down}.sql`：`vector_rebuild_run`（tenant/run_id PK、server-owned `vrr-v1-` run id、projection fingerprint hex64、mode/phase 状态机 CHECK、keyset cursor、attempt/max_attempts、scan/enqueue/tombstone 计数、lease owner/epoch/fence、safe error category allowlist、deadline；ready/lease-expiry/tenant 索引）。既有 migration 未修改；`memory.vector_ref`、job_queue、execution_result、reply outbox、Knowledge schema 未触碰。
- `trpcservice/vector/rebuild/model.go`：Run/Phase（pending→scanning→scanned→completed|failed|cancelled）、Category allowlist、`ProjectionFingerprint`（server-owned，含 model/version/schema/dimension）、`NewRunID`、bounded `Config`（batch/max batches/max attempts/lease TTL/run timeout/reconcile rounds/inspect 上限/orphan 策略开关）、`ReconcilePolicy`（默认 DryRun）、`Report` 分类桶（consistent/missing/stale/tombstoned_present/orphan/invalid/wrong_projection）。
- `trpcservice/vector/rebuild/postgres.go`：`PostgresRunRepository`——Create/Get/Claim（pending、过期 scanning 重claim、scanned re-claim；attempt 预算耗尽 fail-closed）、`AdvanceCursorTx`（caller 事务内 fenced cursor 推进 + 计数，0 rows → ErrConflictOwner）、`MarkScanned`/`Complete`/`Fail`（全部 lease-fenced）、`Cancel`、`Counts`。
- `trpcservice/vector/rebuild/coordinator.go`：`Coordinator`——keyset 分页扫描（`memory_id > cursor ORDER BY memory_id LIMIT batch`，无 OFFSET、无长事务、无表锁）；**每批 "enqueue tasks + cursor 推进" 同一 PostgreSQL 事务**；active→upsert task、tombstone→delete task（复用 P1-06A stable document ID、P1-06C dedup 与 head guard）；crash/resume 从 durable cursor 恢复；batch 上限、deadline、bounded backoff（无 fixed sleep、无 retry-until-success）；`Reconcile`（bounded identity 检查：missing/stale/tombstoned-present/wrong-projection/orphan/invalid 分类；repair 一律经 `RedriveTx` 走既有 task 边界，不直接写 Milvus；orphan 默认 report-only，仅当 server-owned ID/SourceRef 完整可重算且策略显式开启才安全 delete）；`ConvergeAndComplete`（bounded rounds 内零漂移且无非终态任务才 complete——**scan 完成不等于成功**）。
- `trpcservice/vector/inspector.go`：additive `DocumentIdentity` + `IdentityReader` 内部维护边界（只读严格受控 scalar identity，无 vector/content；不改 P1-06A public VectorStore）。
- `trpcservice/vector/milvus/inspector.go`：`InspectIdentities`/`ListIdentities`（tenant predicate 由可信 context 生成；字段 allowlist；Malformed 行 fail closed；不泄漏 SDK 类型）。
- `trpcservice/vector/rebuild/router.go`：server-owned projection registry——按 request ref 重算 fingerprint 路由 Upsert/Delete，identity 读跨注册 projection 合并，Search/Close fan-out；caller 不能选择 projection。
- `trpcservice/vector/task/worker.go`：additive 可选 `EmbedderRegistry`（per-projection embedder 解析；nil 保持单 embedder 行为；状态机/lease/fence 核心语义未变）。
- `cmd/trpc-service/vector_composition.go`：`REBUILD_ENABLED` 默认 disabled——disabled 不构造任何组件；enabled 复用 worker/retrieval 同一 fail-closed 依赖集（含 Embedder seam/registry）；production 无触发路径（无公共/admin API），rebuild 触发路径 NOT IMPLEMENTED。
- 测试：`rebuild_test.go`（unit ordinary/race）、`integration_test.go`（真实 PG/Redis：原子批次、回滚、resume、fencing、attempt 预算、tenant 隔离、dry-run、inspector outage）、`milvus_e2e_test.go`（真实 Milvus 端到端：scan→worker→drift 注入→repair→收敛→tombstone→projection target→cutover BLOCKED）。

## 验证证据（GOFLAGS=-mod=readonly，全部远程实测）

- rebuild unit ordinary/race：PASS。
- rebuild PostgreSQL/Redis integration ordinary/race：PASS（含 keyset cursor 原子性、crash-resume、fencing 拒绝旧 owner、attempt 预算、tenant A/B 隔离、dry-run 无 task、inspector outage→ErrUnavailable 且恢复后收敛）。
- rebuild Milvus E2E ordinary/race：PASS——scan→worker（deterministic test embedder，仅 fixture）→derived index 写入；直接删除派生文档注入 missing 漂移→dry-run 报告→repair（RedriveTx 经 task 边界）→worker 重投影→收敛→completed；tombstoned-present→delete task 修复→收敛；pre-provisioned target rebuild（v2 projection registry+per-projection embedder registry）→收敛→completed；**cutover: BLOCKED**。
- P1-06A/B/C/D regression：vector 全部包 ordinary/race、task/retrieval/milvus integration ordinary/race、storage 全部、cmd/trpc-service ordinary/race：PASS。
- P1-04 concurrent identity 5/5 PASS（accepted=2/winner=1 保持，历史 [401,202] 保留）；P1-05/P0-09 affected PASS。
- full ordinary 31 包 PASS；full race 31 包 PASS；DATA RACE 未观察。
- vet（ordinary+integration）PASS；gofmt clean；targeted diff-check 0 新增（全局 510 条历史 findings 保持不动）。
- migration lifecycle：`TestPostgreSQLMigrations`/`TestCommandFailsClosedOnMigrationMismatch` 随 000008 机械性更新（checksum/failed 清单补 000008 真实文件对，broken probe 顺延 000009）后 PASS；真实服务二进制经 migration 1–8 启动 ready。

## 资源与安全

- 本轮 owner/run 资源（`p106e-milvus-*`、`p106e-reg-*` 及 full-suite 期间 `p006-fi-*` DockerLab 泄漏）全部 label 验证后删除：containers/networks/volumes=0；owned collections 随容器删除；无 prune、未触碰未知资源。
- 本轮 Telegram/Lark/Model Provider/Embedding Provider/Production Milvus requests：`0`（Historical Telegram requests: >0, prior explicitly authorized phase）；DeepSeek 凭据未使用（启动测试沿用既有非空校验路径）；local Milvus/PG/Redis 请求仅限本轮 owner-scoped localhost。
- 日志/错误/metrics 不含 content/vector/query/DSN/credential/fence 值（分类断言覆盖）。
- scanner：govulncheck/gitleaks/trivy/deadcode/jscpd/opengrep UNAVAILABLE/NOT RUN；历史 findings 保留；security clean NOT CLAIMED；release/security state: partial。
- 无 commit/push。

## 未实现 / BLOCKED / NOT RUN

- rebuild/reconciliation 的生产自动触发（scheduler/admin API）：NOT IMPLEMENTED（默认 disabled，无请求路径）。
- projection cutover/alias 协议：BLOCKED（pre-provisioned target rebuild 已验证；未实现原子切换，不自动 drop 旧 projection）。
- production Embedder：NOT IMPLEMENTED；Production Milvus/Cloud IAM/TLS/HA：NOT RUN；semantic retrieval quality：NOT TESTED；cross-system exactly-once：NOT PROVEN。
- orphan 自动 delete 的生产默认：report-only（策略开关存在但生产组合未启用）。
