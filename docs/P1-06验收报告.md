# P1-06 验收报告

## 1. 当前状态

`P1-06A: verified`。`P1-06B: blocked/unavailable`，原因是本轮没有可接受的 Milvus SDK/认证 local Milvus prerequisite，且没有添加 SDK；`P1-06C: not started`；`P1-06D: blocked by P1-06C`；`P1-06E: blocked by P1-06B/P1-06C/P1-06D`。因此 `P1-06: partial`，不能由 A 推导总阶段 verified。

## 2. Durable source evidence

当前 durable source 只有 `trpcservice/memory/model.go` 的 `memory.Memory` 和 `trpcservice/session/model.go` 的 `session.Summary`，对应 `migrations/000001_initial.up.sql` 的 `memory`、`summary` 表。当前 `trpcservice/storage/postgres` 没有 Memory repository，也没有 Knowledge package/model/repository；本轮没有臆造 Knowledge fact、没有新增事实表。

`memory.Memory` 的 source fields 为 tenant、memory ID、scope/scope ID、session ID、kind、content、version、source sequence、deleted 和 timestamps。`MemorySource` 只读取这些事实生成 projection input；Summary 没有接入 vector projection。

## 3. vector_ref

`memory.Memory.VectorRef` 与 PostgreSQL `memory.vector_ref` 已存在，但 grep 未发现当前生产读写。它继续被视为 opaque compatibility marker；没有将它当作 Milvus primary key、没有覆盖旧值、没有迁移旧值。未来如需接入必须使用独立 projection/version 策略并重建。

## 4. 上游 API 核对

`tRPC-Agent-Go v1.11.2` 的 `knowledge/document/document.go` 定义 `Document{ID,Name,Content,EmbeddingText,Metadata,CreatedAt,UpdatedAt}`；`knowledge/vectorstore/vectorstore.go` 的 `VectorStore` 暴露 `Add/Get/Update/Delete/Search/DeleteByFilter/UpdateByFilter/Count/GetMetadata/Close`，SearchQuery 可带 Vector、Limit、MinScore 和 Filter；`knowledge/embedder/embedder.go` 的 `Embedder` 暴露 `GetEmbedding`、`GetEmbeddingWithUsage`、`GetDimensions`。

这些 API 仅作为上游参考。本轮 `trpcservice/vector` contract 不暴露上游 SDK 类型，特别不接受上游 raw filter 作为公共业务接口。

## 5. P1-06A implementation

新增 `trpcservice/vector/contract.go`、`embedding.go`、`contract_test.go`。

- `VectorDocumentRef` 表达 tenant、source type/id、projection scope、source version/sequence、content hash、operation、tombstone、model/version、dimension 和 schema version。
- `vd-v1-` identity 是 server-owned SHA-256 digest，包含 tenant/source/projection/model/version/dimension/schema，不包含 content hash/version/sequence/operation；retry 和 source update 复用同一 ID，model/schema/dimension 变化不会复用不兼容 projection。
- `BuildUpsertRequest` 和 `BuildDeleteRequest` 生成 server-owned request；delete 使用 tombstone，不保存正文。
- `SafeMetadata` 是 bounded allowlist，只保存 tenant、source type、source fingerprint、version/sequence、content hash、model/version、dimension 和 schema；不保存 source content、raw source ID、prompt/history、provider body、credential 或 vector。
- `BuildTenantFilter` 只能从 validated `TenantContext` 创建；SearchRequest 没有 tenant、collection、partition、raw expression 或 arbitrary filter 字段；request ownership 校验拒绝跨 tenant。
- `BackendPolicy.Vector=none` 的 `ResolveBackend` 返回无 client 的 `DisabledStore`，不调用 factory；enabled `milvus` 缺 endpoint reference、collection、credential reference、model/version、dimension、schema 或 timeout 等配置时 fail closed，不回退 fake/memory/silently disabled。
- `DeterministicEmbedder` 为 bounded fixed-dimension local fake，model/version 明确，遵守 cancellation/deadline，不读 credentials、不发 Model Provider request；它不代表真实 embedding 语义质量。

## 6. B-E 状态

B 没有 adapter、schema/index/readiness/upsert/delete/search、raw SDK error conversion 或真实 local Milvus evidence，标记 `BLOCKED/UNAVAILABLE`。C 没有 vector task migration/repository、idempotency/lease/fencing/DLQ/worker，标记 `NOT STARTED`；既有 Outbox/Queue/P0-09F 不被当作 vector task。D 没有 retrieval/hydration/fallback service，标记 `BLOCKED`。E 没有 rebuild/reconciliation/resume/version migration，标记 `BLOCKED`。

## 7. SDK 方案门禁

当前 `go.mod` 没有 Milvus SDK。已核对：

- `github.com/milvus-io/milvus/client/v2@v2.5.0`：Go 1.21，仓库 LICENSE 为 Apache-2.0；上游 client v2 API 有 context-aware `Insert`、`Upsert`、`Delete`、`Search` 和 collection/index/admin operations，覆盖目标能力；但 module 依赖 Milvus proto/pkg、gRPC、protobuf、etcd、OTel、Kubernetes 等较大的 transitive tree，和当前已有依赖存在版本/安全审查 blast radius。
- `github.com/milvus-io/milvus-sdk-go/v2@v2.4.2`：Go 1.17，仓库 LICENSE 为 Apache-2.0；API 覆盖 collection/schema/index、insert/upsert/delete/search/filter 和 context；但上游已标记旧模块 deprecated，且依赖较老 gRPC/protobuf/x/text，回滚容易但不适合作为新接入默认。
- 标准库 `net/http` 调 Milvus REST v2：无新增 module、Go 1.21 兼容、无额外 license/transitive dependency；能够覆盖 collection/schema/index/upsert/delete/search/filter，但必须自行维护 REST schema、bounded JSON、认证、错误分类和协议兼容，且 REST 的 filter/ids 互斥语义需要额外防御。

本轮未能在不引入较大依赖且无真实 authenticated Milvus prerequisite 的条件下完成安全选型，因此没有添加 SDK。新增 SDK 不会等同 production HA/IAM/TLS/integration 通过。

## 8. Verification

- `go test ./trpcservice/vector -count=1`: `PASS`。
- `go test ./trpcservice/vector -race -count=1`: `PASS`，无 `DATA RACE`。
- `go vet ./trpcservice/vector`: `PASS`。
- `gofmt -l` on all new Go files: `PASS`，无输出。
- A affected ordinary `go test ./trpcservice/vector ./trpcservice/memory ./trpcservice/tenant ./trpcservice/audit ./trpcservice/storage -count=1`: `PASS`。
- `go test ./... -count=1`: `FAIL/UNAVAILABLE prerequisites`。vector、memory、tenant、audit、storage 等 package 通过；`cmd/trpc-service` 的 G-D/production tests 在 stage=`prerequisite` 因 `TEST_DATABASE_URL` 缺失；PostgreSQL package `TestCommandFailsClosedOnMigrationMismatch` 在 stage=`real command readiness` 未 ready，进程为 `startup_failure`，未暴露 DSN；owner-scoped Redis cleanup success。
- `go test ./... -race -count=1`: 同样为 `FAIL/UNAVAILABLE prerequisites`，vector package race 通过且无 DATA RACE；既有 G-D/production `TEST_DATABASE_URL` blocker 和 PostgreSQL real-command readiness failure 保留。
- `go vet ./...`: `PASS`。
- gopls: `UNAVAILABLE/NOT RUN`，远程没有 `gopls`。

## 9. Security/scanner evidence

本轮 `gitleaks`、`govulncheck`、`deadcode`、`jscpd`、`trivy`、`opengrep`、`madge` 均 `UNAVAILABLE/NOT RUN`。历史权威报告中的 `govulncheck: FAIL/findings`（5 个 reachable findings）继续有效，未被本轮 unavailable 改写；`security clean: NOT CLAIMED`，project release/security state 为 `partial`。本轮没有依赖变更，因此没有新增依赖 scanner evidence。

## 10. Resources and request counts

本轮没有创建 Docker、Milvus、PostgreSQL、Redis、MinIO 或其他 test resource；`Milvus resources: 0`。未创建 resource 因而没有 owner cleanup；未知资源没有清理。`Lark requests: 0`、`Telegram requests: 0`、`Model Provider requests: 0`。

## 11. Compatibility and forbidden scope

`/api/chat` 未修改，继续保持 `MemoryStore -> platform.Runner -> EchoResponder` 同步兼容。没有修改 P0-09F、Queue、Worker、Completion、Outbox、Dispatcher production core algorithm，没有将 vector operation 放进 PostgreSQL transaction，没有声称 PostgreSQL 与 Milvus exactly-once，没有把 vector hit 当业务事实，没有把 fake embedding 当真实 provider，没有把 local/fake evidence 当 production Milvus evidence。

P1-05 Approval Admin/API、durable policy/approval/budget/audit persistence、Redis policy coordination、real Tool/provider、real embedding/provider、cloud Secret Manager/KMS/Vault/IAM、complete DLP/content moderation、provider-side dedup、cross-system exactly-once、production Milvus HA/IAM/TLS 和自动 collection lifecycle 均 `DEFERRED` 或 `NOT IMPLEMENTED`。

## 12. Required durable state

`P0-09G-R: verified (defined P0 closure boundary)`。

`P0-09: verified (defined durable, assembly and recovery boundary)`。

`P1-04: verified (functional Binding/Identity/SecretRef boundary)`。

`P1-05: verified only as its defined functional Tool Policy/Guardrail/redaction boundary`。

`P1-05 project release/security state: partial`。

`P1-06A: verified`；`P1-06B: blocked/unavailable`；`P1-06C: not started`；`P1-06D: blocked`；`P1-06E: blocked`；`P1-06: partial`。
## Webhook 并发回归分类

本轮按用户授权，仅使用 owner/run-scoped PostgreSQL 16 与 Redis 7 本地测试依赖；协议 readiness 为 `PASS`，测试后 owner-scoped containers/networks/volumes/pending cleanup 均为 `0`。未创建 Milvus resource。

源码确认失败 assertion 位于 `cmd/trpc-service/production_test.go:562-564`：两个相同合法 webhook 请求均应返回 `202 Accepted`；随后测试仍要求 runner calls 为 `1`，并由 `assertSingleTenantFacts` 严格要求 `job_queue=1`、`execution_result=1`、`outbox_message=1`。该期望没有降低 logical-winner 或 durable-side-effect 断言。

不改仓库的 overlay 诊断将失败路径定位为 `Ingress.Handle` identity 分支的 `identityErr`；category-only 分支为 PostgreSQL unique conflict，经 `registryError` 映射为 `storage.ErrConflict`，安全响应为 `[401,202]`。失败时 tenant-scoped 快照为 `identity_rows=1, claims=1, jobs=1, executions=0, outboxes=0, audits=1`；未发现 duplicate durable fact。通过运行验证 `runner=1` 且 jobs/executions/outboxes 均为 `1`。

targeted ordinary 有界 10 次复现为 `5 PASS / 5 FAIL`；失败均为同一 `concurrent accepted responses=1`，targeted race 本轮一次为 `PASS`，另一次有界 overlay race 无 DATA RACE 但仍暴露同一 identity conflict。该模式不是启动顺序、readiness、fixture contamination 或 test expectation defect，而是 `TenantRegistry.ResolveIdentity` 并发 identity upsert 的独立 production regression，分类为 `D: Production regression`，属于 P1-04 identity repository boundary。

本轮没有修改 production、test、P0-09F、Queue、Worker、Completion、Outbox、Dispatcher、P1-04、P1-05 或 P1-06A；没有修改 timeout、删除 assertion 或改变 logical-winner 语义。最新两次完整 synthetic 前置下的 `go test ./... -count=1` 实际为 `PASS`，但此前完整前置下的失败和 targeted 10 次可重复 regression 仍保留，故 ordinary regression gate 不关闭，不能以一次 full PASS 覆盖未修复 blocker。

`P1-06A` 仍为 `verified (contract/projection/fake-embedding boundary)`；`P1-06B` 仍为 `blocked/unavailable`；C/D/E 未推进。Lark、Telegram、Model Provider、Embedding provider requests 均为 `0`；Milvus resources 为 `0`；没有 production Milvus claim。
## P1-04 concurrent identity resolution 当前修复与验收

当前权威状态为：`P1-04: verified (functional Binding/Identity/SecretRef boundary)`；`P1-04 concurrent identity resolution: verified`。本节不覆盖历史 failure：原始 `TestProductionPostgresWebhookFailureWindows/duplicate_concurrent_webhook_has_one_logical_winner` 曾出现 `concurrent accepted responses=1`、`[401,202]`，有界 ordinary 为 `5 PASS / 5 FAIL`；失败快照为 `identity_rows=1, claims=1, jobs=1, executions=0, outboxes=0, audits=1`，根因是 `TenantRegistry.ResolveIdentity` 并发 identity insert 的 unique conflict。

修复位于 `trpcservice/storage/postgres/tenant_registry.go`：使用 `(tenant_id, channel, binding_id, external_user_id)` 的原子 `ON CONFLICT (...) DO UPDATE`（保留 `last_seen_at/updated_at`）；仅在 upsert 未返回行或明确 `user_identity_pkey` 冲突时，对完整 canonical key 做一次 bounded 查询。明确识别的 `user_identity_pkey` 冲突也仅允许进入同一 exact-key 查询；查不到 canonical row、identity ID/internal user ID 不匹配、scope/chat/thread 不匹配或其他错误均 fail closed。没有把所有 `23505`、`storage.ErrConflict` 或 `401` 转成成功，也没有修改 immutable fields。

新增 PostgreSQL barrier+WaitGroup 并发 resolver、canonical constraint、unknown primary-key conflict、immutable identity mismatch 和 category-only error tests。修复后 targeted webhook ordinary `-count=10`、targeted race `-count=10`、repository identity ordinary/race、`cmd/trpc-service` ordinary/race、`trpcservice/storage/postgres` ordinary/race、tenant/gateway/audit/tool/queue/execution/outbox/worker affected ordinary/race、full ordinary/race 均 `PASS`；原有 webhook accepted=2、runner=1、logical winner/durable exactly-one assertions 保持并通过，未观察 duplicate execution/outbox，`DATA RACE` 未观察。

本修复不修改 P0-09F、Queue、Worker、Completion、Outbox、Dispatcher、Ingress contract、P1-05、P1-06A、`/api/chat`、migration、`go.mod` 或 `go.sum`；没有新增 migration/dependency/config，P1-06B 仍 `blocked/unavailable`，C `not started`，D/E `blocked`。

本轮所有 owner-scoped PostgreSQL/Redis test resources 均已 cleanup：containers/networks/volumes/pending 为 `0`；没有 Milvus resource。Lark、Telegram、Model Provider、Embedding provider requests 均为 `0`；历史 govulncheck findings 保留，`security clean` 未声明，project release/security state 仍为 `partial`。

## P1-06B final SDK/API gate addendum

The final source and module-cache audit is recorded below. Cache presence is not a project dependency, build, authentication, readiness, or integration evidence.

### v2.5.0 client source evidence

- `go list -m -json` with `GOPROXY=off` found cached metadata for `github.com/milvus-io/milvus/client/v2@v2.5.0`: Go `1.21`, module time `2025-02-14`, Apache-2.0 LICENSE, source tag `client/v2.5.0`.
- The cached `milvusclient` source provides `New(context.Context, *ClientConfig)`, `Close(context.Context)`, collection/schema, index, load-state, `Insert`, `Upsert`, `Delete`, `Search`, `Query`, and `Get` operations.
- `ClientConfig` exposes address, username/password, API key, TLS, and gRPC dial options. The metadata interceptor encodes credentials as authorization metadata; the application would still own SecretRef resolution, TLS trust policy, deadlines, and log redaction.
- Client construction performs `Connect` unless `DisableConn` is set. The default unary retry covers `Unavailable` and `ResourceExhausted` with six attempts and exponential backoff; this is not yet the repository's bounded operation policy.
- The cached `go.mod` has 12 direct and 95 indirect requirement lines, including Milvus proto/pkg, gRPC/protobuf, etcd, OpenTelemetry, and Kubernetes-related dependencies. This requires a complete dependency, license, and security review before selection.

### v2.4.2 SDK source evidence

- Cached metadata for `github.com/milvus-io/milvus-sdk-go/v2@v2.4.2` confirms Go `1.17`, module time `2024-10-24`, Apache-2.0 LICENSE, and the README compatibility table's Milvus `2.4.x` to SDK `2.4.0` recommendation.
- Its `Client` interface provides collection/schema/load, index, partition, `Insert`, `Upsert`, `Delete`, `DeleteByPks`, `Search`, `Query`, and `Close`. Search accepts collection, partitions, expression, output fields, vector field, metric, and topK, so the adapter would have to close a broad caller-controlled surface.
- Its config exposes credentials, API key, TLS, and dial options. The cached source includes up to six unavailable/resource-exhausted retries plus a separate rate-limit retry policy with a maximum of 75 attempts and a three-second backoff cap.
- The cached `go.mod` has 11 direct and 19 indirect requirement lines, including older gRPC/protobuf/x-series dependencies. The module metadata inspected here does not provide a `Deprecated` field, so this report records it as an older compatibility line and does not claim an unverified deprecation label.

### REST and adapter boundary

- `net/http` would add no module, but no executable Milvus server REST schema/version/auth evidence was available in this audit. A REST adapter would still need bounded JSON, TLS/auth, collection/schema/index, upsert/delete/search schemas, filter/ID exclusivity checks, and provider error classification.

- The existing `trpcservice/vector` seam is suitable for resource-free unit tests: `VectorStore` exposes only `Ready`, `Upsert`, `Delete`, `Search`, and `Close`; tenant scope comes from trusted context; `SearchRequest` has no tenant, collection, partition, or raw filter; and document identity, metadata, model/version/dimension/schema, and tombstones are server-owned.
- A provider adapter is not justified yet. Without a selected SDK or REST protocol, real collection/schema/index constraints, and provider error mapping, a resource-free adapter would test only local field assembly and could not establish readiness, retry/timeout, reconnect, authorization, or provider operation semantics.
- Therefore this round adds no SDK-specific interface, fake Milvus protocol, or synthetic integration harness. It preserves the provider-neutral P1-06A contract.

### Final decision

- `P1-06A = verified (contract/projection/fake-embedding boundary)`.
- `P1-06B = BLOCKED/UNAVAILABLE`; `Milvus adapter = NOT IMPLEMENTED`.
- `Milvus resources = 0`; `新增 dependency/go.mod/go.sum/migration = 0`.
- P1-06C remains `NOT STARTED`; P1-06D and P1-06E remain `BLOCKED`.

### Verification interpretation

- The current final P1-04 ordinary and race evidence is recorded in the concurrent identity resolution section above; the historical `[401,202]` failure remains preserved separately and is not rewritten as a readiness or test-expectation issue.
- Full ordinary/race, affected package ordinary/race, targeted repository/webhook ordinary/race, vet, gofmt, and targeted diff checks passed after the identity fix; no `DATA RACE` was observed.
- gopls and the listed scanners remain unavailable in the current environment. Historical `govulncheck` findings remain effective, `security clean` is not claimed, and no Milvus readiness or real operation was run.

## P1-06B 本轮远程验收状态

状态：`BLOCKED/UNAVAILABLE`。本节是当前状态补充，保留前文 P1-04 历史 `[401,202]` 并发失败和修复记录。

远程仓库为 `/home/pi/trpc-agent-service`，接手时 `go.mod`/`go.sum` 哈希分别为 `6cfe40687d106a10bcbd31d5a3f92d18525b8afe84bf616795da0e0e0b73cba6` 和 `1a11982140ec84e2b6b4e5f40e7fb338a0844333dbdcb7a555e21b25623a3e71`；既有工作区修改和 global `git diff --check` 的 512 条 findings 均保留。

SDK 源码复核使用 `github.com/milvus-io/milvus/client/v2@v2.5.0`：Go 1.21、Apache-2.0；确认了 `New`、认证 headers、collection describe、index describe、load state、upsert、delete、search、result mapping 和 `Close` API。SDK 默认 blocking dial，并对 `Unavailable`/`ResourceExhausted` 有最多 6 次 gRPC retry；adapter 只叠加 server-owned bounded context，不叠加应用层 retry。

dependency dry-run：`PASS`。带真实 importer 的 scratch module 与实际 module graph 一致；Go directive 未变，pgx、go-redis、x/text、gRPC、protobuf、OTel、trpc-agent-go 版本未变。实际 delta 只来自 SDK/proto/pkg、etcd/OTel 等传递依赖以及可解释的 MVS/direct-import 调整，没有使用 `go mod tidy`。

adapter 已新增于 `trpcservice/vector/milvus`。Milvus SDK 类型只出现在 provider-specific package 的私有 seam 和实现中；`VectorStore`、Document/request/result、tenant model、gateway/channel/tool public contract 未修改。collection、schema、model/version、dimension、metric、index 名和 timeout 均由 validated server-owned config 或固定 allowlist 决定，生产 `Ready` 不创建 collection/index。

schema 使用 server-owned `vd-v1-` document ID、tenant、source projection markers、model/schema compatibility fields 和 float vector；不存储 content、prompt/history、token、credential、DSN、raw provider response 或 query vector。Search 使用 SDK expression template 绑定可信 tenant，Delete 使用仅由已验证 tenant 和 document ID 生成的精确 expression；caller 没有 raw filter、collection、partition、namespace 或 tenant 字段。

Upsert/Delete/Search 的 unit mapping、bounds、stable identity、tombstone、score/metadata defensive mapping、cross-tenant rejection、context 分类、raw error category-only redaction 和 Close idempotency 已覆盖。unit ordinary：`PASS`；unit race：`PASS`；vector package vet：`PASS`；受影响 Go 文件 gofmt 和 targeted diff-check：`PASS`。

authenticated local Milvus readiness、真实 schema/index/load、real upsert/delete/search、tenant isolation、mismatch、restart/reconnect：`NOT RUN`。固定 `milvusdb/milvus:v2.5.0` 拉取因宿主 `/` 仅剩约 1.9 GB 而失败，报错为 `no space left on device`；没有创建本轮 container/network/volume，也没有用 fake 代替 integration。

affected ordinary 和 full ordinary：`FAIL`，原因是既有 PostgreSQL/lifecycle prerequisites 未提供 `TEST_DATABASE_URL`，以及 storage migration command 的既有 startup failure；不是 Milvus unit PASS。affected race 和 full race：`FAIL`，同一 prerequisites；完成的 race 输出未观察到 `DATA RACE`，但不能覆盖整体 FAIL。integration-tag compile：`PASS`；integration execution：`SKIP/NOT RUN`。

scanner 状态：`govulncheck`、`gitleaks`、`trivy`、`deadcode`、`jscpd`、`opengrep`、`madge` 均为 `UNAVAILABLE/NOT RUN`；历史 5 项 reachable findings、约 252 exact clones/2038 duplicated lines/5.29% 继续保留，`security clean` 未声明。

owner cleanup：本轮 Milvus owner container/network/volume/collection 均为 `0`，因为 provision 在镜像层提取前失败；既有 PostgreSQL/Redis testinfra 的 owner resources cleanup 输出为成功。没有执行 Docker prune，也没有删除未知资源。

真实请求计数：Lark `0`、Telegram `0`、Model Provider `0`、Embedding Provider `0`、Production Milvus `0`。未使用 production endpoint 或 credential。

最终阶段状态：P1-06A `verified only within its defined contract/projection/fake-embedding boundary`；P1-06B `blocked/unavailable`；P1-06C `not started`；P1-06D `blocked`；P1-06E `blocked`；P1-06 `partial`。P0-09、P0-09G-R、P1-04 concurrent identity fix 和 P1-05 defined functional boundary 保持原状态。


## P1-06B 存储释放后复验（本轮最终记录）

前置：用户释放磁盘并通过 preflight（`/` 约 56GiB 可用、`/home` 约 309GiB 可用、inode 约 1%，Docker Root Dir `/home/docker-data/docker` 位于 `/home` 且满足门禁）；本轮未执行任何 prune，未删除未知资源。run ID `p106b-20260903-164816`；owner labels `p106b.milvus.owner=p106b-milvus`、`p106b.regression.owner=p106b-regression`。

镜像（固定 tag，etcd/MinIO tag 取自 Milvus v2.5.0 官方 standalone compose 核实）：`milvusdb/milvus:v2.5.0`（RepoDigest sha256:129ad13dd76e7ae8eada4019396fa5d21550a4ed8c19addf080c6c10567b2259）、`quay.io/coreos/etcd:v3.5.14`（sha256:42106a001d1be7fd22e48830071ceae0fe760ff2c5df8c76c0e3b586d8a1f0fc）、`minio/minio:RELEASE.2023-03-20T20-16-18Z`（sha256:6d770d7f255cda1f18d841ffc4365cb7e0d237f6af6a15fcdb587480cd7c3b93）、`postgres:16-alpine`、`redis:7-alpine`。拓扑为 Milvus standalone + 独立 etcd/MinIO + 唯一 network/volumes；host 端口仅绑定 127.0.0.1。初期随机 host 端口在本轮 owned container restart 后被 Docker 重新分配（32768→32769），为使 bounded restart/reconnect 可判定，重建 owned milvus container 并固定 `127.0.0.1:19530`。

认证：`COMMON_SECURITY_AUTHORIZATIONENABLED=true`；synthetic local credential 仅存放于 repo 外 600 权限临时文件并在清理阶段删除。错误凭据与匿名连接均被即时拒绝（0.00s，非超时歧义），authenticated readiness 由真实 SDK 构造 + Ready 证明。

bounded readiness：etcdctl endpoint health、MinIO health/live、Milvus /healthz 连续 3 次、authenticated SDK construction、fixture provision（Strong consistency collection + server-owned collection properties + Flat/COSINE index + load state）全部通过；无 fixed-sleep 成功条件。

真实 integration（`-tags=integration`，0 skip，跑两次 ordinary/race）：authenticated Ready、collection/schema/index/load 验证、real Upsert、same-ID 重复 Upsert、真实 Search（topK/minScore/ID+score mapping）、tenant A/B 检索隔离、cross-tenant upsert/delete 拒绝、exact delete、repeated delete 返回 unknown（明确不声称 provider-side exactly-once）、wrong dimension/model/schema fail-closed、cancelled context、Close 幂等与 post-Close 拒绝、owned collection cleanup。integration ordinary `PASS`（36.8s）、integration race `PASS`（35.5s）、`DATA RACE` 标记 0。

restart/reconnect：仅 stop/start 本轮 owned milvus container（etcd/MinIO 保持运行）；停机期间 Ready 安全失败、无误报 write success；恢复采用 factory 重建 client（`factory rebuild after local process restart: PASS`，非 same-client 自动重连）；原 collection 与 record 保留；恢复后 Ready/Upsert/Search/Delete 全部通过。

本轮 adapter 最小修复（允许范围内）：请求的 `Dimension/Model/ModelVersion/SchemaVersion` 与 server-owned config 不一致时在 SDK 调用前 fail-closed（此前依赖后端拒绝会返回 unknown 或被元数据字段静默接受），并新增 `TestRequestMustMatchServerOwnedBackendConfig`；fixture 补 collection properties、Strong consistency、bounded construction/restart 轮询、repeated-delete unknown 断言、wrong/anon credential 拒绝测试、cleanup 重试 drop。P1-06A public contract 未修改。

诊断记录：affected ordinary 首轮暴露 p006c/G-D 真实命令子进程缺 `BOOTSTRAP_*`/`P009GC_*` synthetic env 导致 `startup_failure`（历史 blocker 根因之一），以外层 synthetic env 补齐修复。G-D `startup_dependency_failure` 曾因本机 Mihomo fake-ip DNS 将 `g-d-invalid.invalid` 解析到黑洞地址（`fdfe:dcba:9876::1c`，路由 `dev Mihomo`）悬挂至 20s（测试窗口 10s）；用户添加 `/etc/hosts` 条目后该测试 `PASS`。该 hosts 条目是用户侧系统修改，是否移除由用户决定。

回归结果：affected ordinary `PASS`；affected race `PASS`（17 包，DATA RACE 0）；full ordinary `PASS`（28 包，0 skip）；full race `PASS`（28 包，DATA RACE 0）；`go vet ./...` `PASS`；本轮文件 gofmt `CLEAN`；targeted diff-check（tracked + no-index untracked）`CLEAN`；global 512 条既有 findings 保留未清理。`TestProductionPostgresWebhookFailureWindows` 全部 5 子测试 `PASS`（含 `duplicate_concurrent_webhook_has_one_logical_winner`；accepted=2/winner=1/runner=1/durable facts 唯一 断言在测试内部成立），P1-04 历史 `[401,202]` 证据保留。

回归环境：owner-scoped PostgreSQL 16（127.0.0.1:5432）+ Redis 7（127.0.0.1:6379/15）；用户提供的 provider/webhook 配置按服务契约归一（`MODEL_PROVIDER=deepseek` 归一为 `runner`，DeepSeek endpoint/model/key 原值保留；synthetic BOOTSTRAP/P009GC secrets 仅存在于 repo 外 600 权限临时文件并已删除）。

scanner：`govulncheck`、`gitleaks`、`trivy`、`deadcode`、`jscpd`、`opengrep`、`madge` 均 `UNAVAILABLE/NOT RUN`；历史 5 项 govulncheck findings 与 jscpd 记录保留；security clean 未声明。

cleanup：teardown 前 owned collections=0（fixture cleanup 成功）；5 容器/2 网络/4 卷全部 label 验证后删除；owner 资源计数 0/0/0/0；含 credential 的临时 env 文件已删除；清理后磁盘 `/` 约 56GiB、`/home` 约 307GiB 可用。

真实请求：Lark `0`（`LARK_B1_REAL` 未设置，real gate 未执行）；Telegram real gate 按用户本轮明确提供并要求的 `TELEGRAM_B2_REAL=1`（真实 bot token/chat ID）在 full ordinary 与 full race 中实际执行，真实 Telegram API 请求大于 0（gate 内部未导出精确计数）；Model Provider `0`（无任何测试发起真实 chat completion）；Embedding Provider `0`；Production Milvus `0`。local Milvus 测试请求仅指向本轮 `127.0.0.1` fixture。

最终状态：`P1-06A verified only as its contract/projection/fake-embedding boundary`；`P1-06B verified only as a local Milvus adapter/integration boundary`；`P1-06C not started`；`P1-06D/P1-06E blocked`；`P1-06 partial`。Production Milvus `NOT RUN`；Cloud IAM/TLS `NOT RUN`；Milvus HA/failover `NOT RUN`；provider-side exactly-once `NOT PROVEN`；real embedding provider `NOT RUN`；semantic retrieval quality `NOT TESTED`；vector task/worker、retrieval/hydration、rebuild/reconciliation、Knowledge durable source、PostgreSQL Memory repository 均 `NOT IMPLEMENTED`；security clean `NOT CLAIMED`。

本轮未修改：P1-06A public contract、P0-09F/Queue/Worker/Completion/Outbox/Dispatcher 核心算法、P1-04/P1-05 contract、migration、production secret/config；无 commit、无 push。


## P1-06D 附加记录（当前）

`P1-06D injected retrieval/hydration boundary: PASS`；`P1-06D production Memory hydration: NOT IMPLEMENTED`；production closure `BLOCKED/UNAVAILABLE`。检索/水合边界以真实 PostgreSQL 与真实 local Milvus 验证（详见 `docs/P1-06D验收报告.md`）；P1-06A/B/C 既有结论不变；P1-06E 未开始；P1-06 维持 `partial`。


## P1-06D-P 附加记录（当前）

`P1-06D: verified only as a production-composable Memory retrieval/hydration boundary`（production Memory repository、同事务 projection enqueue、SourceProjector、默认关闭的 worker/retrieval composition、真实二进制 fail-closed 均已验证）。`Production Embedder: NOT IMPLEMENTED`；`P1-06E: blocked/not started`；`P1-06: partial`。详见 `docs/P1-06D-P验收报告.md`。


## P1-06E 附加记录（当前）

`P1-06E: verified only as a durable rebuild/reconciliation boundary`；`projection migration boundary: pre-provisioned target rebuild PASS / cutover BLOCKED`；`P1-06: verified only as a disabled-by-default, production-composable derived-vector lifecycle boundary`；`production rollout: BLOCKED`。详见 `docs/P1-06E验收报告.md`。
