# P2-04 验收报告：本地集成、有界压力/故障、安全测试与生产运维手册边界

## 1. 最终状态

`P2-04: verified only as a local integration, bounded stress/failure,
security-test and production-operations-runbook boundary`

执行中断说明：本轮执行期间远端宿主因电池耗尽断电一次（连接中断约
10 小时），恢复后验证：git 树与依赖哈希完整、全部 P2-04 文件在、
Docker 容器以 Exited(0) 保留、/tmp（tmpfs）自动清空。断电不影响已
产生的测试证据；owner 资源清理在恢复后按证据完成（第 23 节）。

## 2. 四个子边界状态

| 子边界 | 状态 | 说明 |
| --- | --- | --- |
| A 跨组件集成和契约回归 | **PASS** | 完整异步链路、PG/Redis restart、P2-02/P2-03 联合回归、configpub、Object/OTel 门禁全部通过（详见第 8 节） |
| B 有界压力/并发/过载/故障恢复 | **PASS** | B1-B5 由 P2-03 套件回归覆盖；B6 新增有界 soak PASS（第 9 节） |
| C 安全负向测试与审计 | **PASS（有保留）** | secret scan/compose 审计/运行时审计/webhook 负向/RLS 审计 PASS；gitleaks/govulncheck/trivy UNAVAILABLE；既有 5 个 reachable findings 保留 → `Project release/security closure: PARTIAL — existing reachable dependency findings remain; no security clean claim` |
| D 运维手册/发布门禁/事故演练 | **PASS** | `docs/P2-04运行文档.md`（25 项 + 23 行事故矩阵 + 发布门禁）+ `scripts/p204_release_gate.sh` 实际执行 PASS；事故处置的本地演练由 compose 故障门禁承担 |

## 3. source audit 范围

docs/implementation-plan.md、project-status.md、ARCHITECTURE.md、
P2-01/02/03 验收与运行文档、P1-09 运行/验收、P1-05/07/08 验收、
P0-09G-R、migrations 000001-000011 up/down、cmd/trpc-migrate、Dockerfile、
Dockerfile.recovery、docker-compose.yml、.dockerignore、.env.example、
deploy/otel-collector.yaml、cmd/trpc-service（main/production/lifecycle/
vector_composition）、web、gateway、admission、ratelimit、queue、worker、
outbox、storage（repository/postgres/redis/coordination）、session、
execution、config、configpub、memory、vector、channels、tool、telemetry、
platform、internal/testinfra。集成链路按 8 条主链记录（见第 8 节矩阵），
每链含入口/authority/事务边界/租户边界/lease-fence/失败分类/重试所有权/
可观察信号/恢复动作。

## 4. files changed

- 新增：`cmd/trpc-service/p204_secret_scan_test.go`、
  `cmd/trpc-service/p204_security_audit_test.go`、
  `cmd/trpc-service/p204_soak_integration_test.go`、
  `scripts/p204_release_gate.sh`、`docs/P2-04运行文档.md`、
  `docs/P2-04验收报告.md`。
- 修改：`docs/implementation-plan.md`、`docs/project-status.md`、
  `docs/ARCHITECTURE.md`（P2-04 状态最小更新）。
- 生产代码零改动：P0-09 durable contract、P1-04/05/07/08、P1-09 默认
  Compose、P2-01 RLS/role、P2-02 backup/restore/replay、P2-03
  admission/rate-limit/capacity 合同全部保持。

## 5. go.mod/go.sum delta

**0**。`sha256sum`：go.mod `50ace4330977e4cc0f57f55965bcaf8a61390d01f8b6c47e1c5d3c49c4d7d5ed`、
go.sum `23cba731c8cc436c8cab0f6e4df247e793e573b2fb3a32206c5e7918157b43f1`
（与 P2-03 关闭基线一致）。未运行 `go get -u`/tidy/任何依赖升级。

## 6. migration delta 和 checksum

**0**。migrations 000001-000011 未改动（22 个 SQL 文件）；broken probe
保持测试内 `000012`；未新增 000013。11 个版本 checksum 由发布门禁/
recover 套件校验（`3a8c6b85…`/`21f92d74…` 为 000011 up/down）。

## 7. config/Compose delta

**0**（本轮未修改 docker-compose.yml/Dockerfile/.env.example；P2-03 的
容量变量与 recovery profile 保持）。`.dockerignore`/`deploy/otel-collector.yaml`
由安全审计测试断言其合同（未修改）。

## 8. 集成矩阵与证据（A）

| 链路 | positive | negative | timeout/cancel | restart/recovery | tenant A/B | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 配置与启动 | compose up ready；migration bootstrap | invalid config fail closed；migration mismatch 拒启动 | 启动预算有界 | 重启后幂等 | bootstrap tenant 单域 | Compose 门禁 + p006c 套件 |
| 2 Webhook 入站 | verify→rate limit→admission→claim→enqueue→202 | 401/400/429/503 分类；被拒不 Claim | ctx cancel 释放 slot | 重启后 202 | 调用方不可覆盖 server-owned 值 | P2-03 套件 + 容量门禁 |
| 3 Queue/Worker/Completion | definer claim→lease→runner→result+Ack+Outbox 原子 | 旧 fence 拒绝；invalid job 拒绝 | deadline/visibility/lease 分类 | SIGKILL/restart reclaim、attempt 递增 | RLS 租户隔离 | P0-09/P2-01/P2-02 套件 + soak |
| 4 Session/Event/Memory | session/event/memory PG authority；P2-02 replay dry-run/repair | gap/parent/payload/watermark 类别 | repair CAS 幂等 | restore 后 replay | 租户隔离（replay 逐租户 GUC） | P2-02 套件回归 |
| 5 Outbox/Dispatcher/Sender | claim/lock→recording sender→complete | 429/5xx/unknown→retry/DLQ；unknown 永不 delivered | lock guard/shutdown 有界 | 新 owner reclaim 全部 pending | 路由仅 server-owned | P0-09E/P2-03 套件 + soak |
| 6 Config publication | draft→validate→publish→rollout→rollback | stale version 拒绝；不可用版本 fail closed | — | rollback 后版本一致 | 租户级 rollout | P1-08 套件（回归重跑） |
| 7 Artifact/ObjectStore | metadata pending→ready；presign gate | missing/mismatch→expired/failed；路径遍历拒绝 | — | — | 租户前缀 | object 套件（本地 MinIO 边界） |
| 8 Observability | ingress→…→sender correlation；低基数指标 | collector outage 不改业务分类 | flush/shutdown 有界 | collector 重启恢复 | 无高基数 label | P1-07 套件 + telemetry e2e |

未证明边界逐链标注于运行文档第 15/21 节与第 26 节清单。

## 9. bounded stress/failure 矩阵（B）

| 项 | 配置 | 结果 |
| --- | --- | --- |
| B1 ingress/admission | global=4/tenant=2（演练）与 16/8（soak）；固定请求数；结果全部可分类；拒绝不 Claim；高水位 ≤ 上限；active 归零；duplicate release/cancel/panic/enqueue failure 不泄漏 | PASS |
| B2 queue/worker | concurrency 不超限；cancel 后不启动新 Runner/Tool；slow execution 无界 goroutine=否；timeout/visibility/lease 分类稳定；stop 后 reclaim、attempt 递增、旧 fence 拒绝；无丢失/重复 | PASS |
| B3 dispatcher/outbox | batch/concurrency 有界；sender 阻塞到 ctx deadline 可回收；lock guard 防 late mutation；retry cap/DLQ cap；429/5xx/permanent/unknown 分类；shutdown 不丢 pending；新 owner reclaim；无内存 payload backlog | PASS |
| B4 pool/依赖 | pool 耗尽 deadline 失败+恢复；PG/Redis stop/restart；readiness/liveness 正确；无无限连接/重试/进程增长 | PASS |
| B5 lifecycle | SIGTERM→draining→新入口 503；bounded stop（load 下有界 drain 类别）；pending 可回收；/healthz//livez 合同 | PASS |
| B6 bounded soak | **新增**：120 操作/并发 6/45s deadline/worker 1/采样周期 20 ops；结果 120×202 全分类；accepted==durable；goroutine 高水位 400、结束无泄漏；heap 高水位 10.7MB；admission 归零；recording sender 120:0；耗时 61.9s | PASS |

全部使用 channel/barrier/事件同步与有界 polling；无 fixed sleep 正确性
同步、无 t.Skip 掩盖、无 unknown-当-success、无超时放大、无请求量缩减。

## 10. security negative-test 矩阵（C）

| 类别 | 覆盖 | 证据 |
| --- | --- | --- |
| C1 Tenant/RLS | 双租户隔离、跨租户拒绝、RLS ENABLE+FORCE、policy USING/WITH CHECK、runtime NOBYPASSRLS/非 owner/无 DDL、schema_migration 不可读、recovery 路径分离 | P2-01 套件（回归重跑）+ P2-02 恢复套件 |
| C2 Input/auth | oversized body(1MiB)、malformed JSON、duplicate/replay webhook、invalid signature/token、caller tenant 覆盖拒绝、invalid event sequence/parent/payload（replay）、TOC 未知对象/checksum 不完整/marker 缺失、restore same-source/非空目标拒绝 | gateway/ingress/recovery 套件 + P2-02 拒绝矩阵 |
| C3 SSRF/网络 | provider base URL 固定 canonical（telegram/lark 常量）；ObjectKey 路径遍历/`..`/反斜杠校验（artifact 模型）；Tool Policy/Guardrail fail closed；provider 429/5xx/unknown 不写成 delivered | channels/artifact/tool 套件 |
| C4 Secrets/脱敏 | **新增 TestP204SecretScan**：14 目标×7 类高信号模式；占位/机制白名单；compose/Dockerfile/.dockerignore 由审计断言 secret-file 注入；测试凭据全为合成占位 | 本轮新增 |
| C5 DoS/资源 | body/payload/batch/retry/queue wait/goroutine/thread/pool acquire/active slot 全部有界（B 矩阵）；cancel/timeout/panic 后释放；rate limit key 与 metric label 无高基数业务 ID | B 套件 + admission/telemetry 测试 |
| C6 容器/Compose | **新增 TestP204ComposeSecurityAudit + TestP204AppContainerRuntimeAudit**：无 latest、loopback-only 发布、非 root（uid 探针）、secret-file 注入、无明文 DSN 于进程 env、healthcheck、默认 profile 无 test-only 服务、Dockerfile/dockerignore 合同 | 本轮新增 |

## 11. RLS/role/catalog 结果

P2-01 套件在 P2-04 回归中重跑：24 表 relrowsecurity+relforcerowsecurity、
`<table>_tenant_isolation` FOR ALL policy、三 SECURITY DEFINER 函数
owner=trpc_claim_owner 且 PUBLIC REVOKE、claim owner NOLOGIN、runtime
NOSUPERUSER/NOBYPASSRLS/非 owner/不可读 schema_migration、双租户隔离、
100 并发租户、移除应用 WHERE 的 probe——全部 PASS。P2-04 未修改任何
role/policy/migration contract。

## 12. scanner availability 和 findings

| 工具 | 状态 | 说明 |
| --- | --- | --- |
| go vet ./... | **PASS** | 无 finding |
| gopls diagnostics | NOT RUN（主机无独立 gopls 调用入口；go vet 覆盖） | — |
| gitleaks | UNAVAILABLE（主机无二进制）；以 TestP204SecretScan（7 类高信号模式）替代性负向扫描 | 替代范围有限，如实标注 |
| govulncheck | UNAVAILABLE（主机无二进制）；既有 5 个 reachable findings 保留：grpc `GO-2026-6061`、x/text `GO-2026-5970`/`CVE-2026-56852`、pgx `GO-2026-5004`/`CVE-2026-41889`、OTel SDK `GO-2026-4394`/`CVE-2026-24051`、go-redis `GO-2025-3540`/`CVE-2025-29923` | 不删除/不掩盖/不归因 P2-04；未执行任何依赖升级 |
| trivy | UNAVAILABLE | — |
| gosec/semgrep | NOT RUN | — |
| dead-code/jscpd/madge | NOT RUN | — |
| `docker compose config -q` | PASS | 默认 profile |
| shell/YAML/SQL 检查 | `bash -n scripts/p204_release_gate.sh` PASS；compose 由 config -q 覆盖；migration 由 checksum/gate 覆盖 | — |
| P2-04 secret scan（测试内建） | PASS | 见第 10 节 C4 |

`Project release/security closure: PARTIAL — existing reachable dependency
findings remain; no security clean claim`

## 13. P2-02/P2-03 回归结果

P2-02：backup/verify/restore/refusal 矩阵/replay dry-run+repair 全部在
integration 套件重跑 PASS（restore 后 RLS/约束/计数/FK/definer/runtime
probes 审计通过）。P2-03：admission/rate-limit/pool/dispatcher/soak/
compose 容量门禁全部 PASS。P2-01：RLS/role 套件 PASS。

## 14. ordinary/race/integration/vet/gofmt/diff-check

| 套件 | 结果 |
| --- | --- |
| `GOFLAGS=-mod=readonly go test ./... -count=1`（42 包） | PASS |
| `GOFLAGS=-mod=readonly go test ./... -race -count=1`（42 包） | PASS（DATA RACE 未观察） |
| `GOFLAGS=-mod=readonly go test -tags=integration ./trpcservice/... ./cmd/... -count=1`（41 包，含 compose 门禁） | PASS |
| `GOFLAGS=-mod=readonly go test -race -tags=integration ./trpcservice/... ./cmd/... -count=1`（41 包） | PASS |
| `GOFLAGS=-mod=readonly go vet ./...` | PASS |
| gofmt（cmd/trpcservice/internal） | PASS |
| targeted `git diff --check`（本轮文件） | 0 findings |
| 全局 `git diff --check` | 460 行历史 findings 保留（README 等既有内容），不归因 P2-04 |
| `bash scripts/p204_release_gate.sh` | PASS（含断电恢复后重跑） |

全量套件环境：本地 owner-scoped PostgreSQL16/Redis 容器
（`p204-integration-1788697280`）提供 TEST_DATABASE_URL/TEST_REDIS_URL；
服务子进程以占位 BOOTSTRAP_*/MODEL_* 启动（Lark/Telegram disabled、
runner 回环端点）。

## 15. PostgreSQL evidence

- 演练/soak 的 durable 不变量：accepted == job_queue 行数（唯一
  execution id）、无重复无丢失（soak 120/120）；
- restart/recovery：PG stop → healthz 503/livez 200；start → fresh pool
  + webhook 202；queue/Outbox reclaim、attempt 递增、旧 fence 拒绝
  （P0-09/P2-02/P2-03 套件 + compose 门禁）；
- atomic completion 无重复事实（结果/Ack/Outbox 三事实关系断言）；
- pool 耗尽有界 acquire（600ms deadline）+ Reset 恢复。

## 16. Redis evidence

- rate limit：健康判定/配额拒绝分类正确；stop → backend unavailable
  fail closed（不当作允许/普通限流）；restart → 同 client 有界恢复、
  窗口计数保留；
- Redis 非 Queue/Outbox/execution result/configpub/recovery manifest 的
  authority（故障演练中业务事实不受影响）；
- 无限重连不存在：client 有界 timeout，恢复经有界轮询验证。

## 17. ObjectStore/OTel evidence

ObjectStore：既有本地 MinIO 边界（metadata pending/ready/failed/expired、
hash/size/MIME、missing/mismatch reconciliation、presign gate、路径遍历
拒绝）在回归中重跑 PASS；本轮未新增字节级证据，`Object Storage byte
backup/restore: NOT INCLUDED` 保持。OTel：本地 collector e2e（traces+
metrics 到达、outage 业务安全、重启恢复）在回归中重跑 PASS；
`Production telemetry collector rollout: NOT RUN` 保持。

## 18. Compose/生命周期 evidence

`docker compose config -q` PASS；`up -d --wait` full stack ready；
`/livez`、`/healthz` 启动/依赖故障/draining 语义正确；默认
vector/retrieval/rebuild/object 行为不变；无 fake sender 进入默认路径；
PG/Redis stop/start 故障演练；SIGTERM 有界停止 + pending 可回收
（attempt 递增、行数不变）；TestComposeLifecycleAndRecovery 26.5s PASS、
TestComposeCapacityProtectionGate 25.6s PASS。

## 19. local observed values（仅本地观测）

- soak：120 操作、并发 6、worker 1、45s deadline；accepted=120；
  goroutine 高水位 400；heap 高水位 10.7MB；全程 61.9s；
- compose 门禁：25.6s（容量）/26.5s（lifecycle）；
- pool 耗尽有界失败 600ms；rate limit Retry-After 实测 31s（窗口余量）；
- dispatcher 有界派发实测约 2/s（concurrency 2 × 1s interval）；
- 恢复耗时：Redis 恢复经有界轮询 ≤90s 内 202；PG 恢复后 healthz 200。

以上均为本地测试观测，不构成 production maximum QPS、capacity、
recovery time、SLO 或 RTO/RTO。

## 20. external request counts

`External provider requests in P2-04: 0`
（Telegram=0、Lark=0、Model=0、Embedding=0、Production Milvus=0。）
用户会话中提供的真实凭据未被读取、未写入任何文件、未用于任何请求。
`Historical Telegram requests: >0, prior explicitly authorized phase` 保持。

## 21. secret/log redaction 结果

- TestP204SecretScan PASS（14 目标、7 类模式、白名单仅合成占位/机制行）；
- compose 安全审计：无明文密码环境注入、secret-file 机制、运行时进程
  env 无 DSN（探针断言）；
- 本报告/运行文档/测试日志均不含 DSN、password、token、tenant/session/
  message/job/execution/outbox/task/document ID、prompt/history、
  provider body、raw SQL/SQLSTATE、ObjectStore key；
- 临时凭据文件（套件 secret 目录）位于 /tmp tmpfs，断电重启后已随
  tmpfs 清空；容器内 secret 为 Docker secret 机制注入。

## 22. resource before/after snapshot

| 项 | P2-04 开始前 | P2-04 结束后 |
| --- | --- | --- |
| containers | 0 | 0 |
| networks | 3（宿主默认） | 3（宿主默认） |
| volumes | 917（历史 831 + P2-03 尾差 86） | 831（历史基线；本轮 156 个匿名卷按创建时间证据清理） |
| images | 13 | 13（p202-recovery-tool:local P2-02 尾差移除；门禁构建镜像由 TestMain/teardown 清理） |
| 后台进程 | 0 | 0 |
| /tmp P2-04 文件 | — | 0（tmpfs 断电清空 + 确认） |

## 23. cleanup 结果

**PASS**：owner-scoped containers/networks/volumes/images/temp files/
processes/pending cleanup 全部为 0。清理仅针对三重证据（owner/run ID、
创建时间、类型与父级关系）匹配的本轮资源：套件容器 2、网络 1、本轮
匿名卷 156（09-06 17:00-21:59 测试窗口）、P2-02 尾差镜像 1。历史资源
（831 个卷、宿主默认网络、既有镜像）未触碰；未执行 prune。

## 24. unresolved findings

- 既有 5 个 reachable govulncheck findings（见第 12 节）——保留，未升级；
- 扫描器 gitleaks/govulncheck/trivy/gosec/semgrep/dead-code/jscpd
  UNAVAILABLE/NOT RUN——覆盖范围受限（已用内建 secret scan/审计测试
  部分替代，如实标注）；
- 无新增未解决生产代码缺陷（全量回归无失败）。

## 25. residual risks

- dependency findings 未升级（需明确授权的依赖 baseline 变更）；
- 扫描器覆盖受限（UNAVAILABLE 替代物为模式扫描，非完整 SAST）；
- 生产环境的一切容量/HA/部署语义未验证（清单见第 26 节）；
- 运维手册为本地/预生产性质，生产运维从未执行。

## 26. 明确的"未证明"和"未执行"清单

`Production integration/deployment: NOT RUN`；`Production capacity
planning and throughput limits: NOT PROVEN`；`Production
autoscaling/HPA/Kubernetes: NOT IMPLEMENTED`；`PostgreSQL HA/failover:
NOT PROVEN`；`Redis Cluster/HA: NOT PROVEN`；`Cross-region
capacity/failover: NOT IMPLEMENTED`；`Provider global quota
certification: NOT PROVEN`；`Production SLO/SLA: NOT PROVEN`；`Production
backup scheduling/retention/encryption/off-site replication: NOT
IMPLEMENTED`；`Production PITR/WAL archiving: NOT IMPLEMENTED`；`Production
restore cutover/failback and RPO/RTO: NOT PROVEN`；`Object Storage byte
backup/restore: NOT INCLUDED`；`Secret Manager value backup/restore: NOT
INCLUDED`；`Milvus index backup: NOT INCLUDED; rebuild only`；`Whole-system
event sourcing/replay: NOT IMPLEMENTED`；`External side-effect
replay/exactly-once: NOT PROVEN`；`Production RLS role/database rollout:
NOT RUN`；`Production Embedder: NOT IMPLEMENTED`；`Production Milvus: NOT
RUN`；`P1-06 production rollout: BLOCKED`；`Production deployment: NOT
RUN`；`Security clean: NOT CLAIMED`；P2-03 保留项（Cross-process
queue-depth/Global fairness/Production capacity limit/per-tenant worker
budget/per-channel sender budget/circuit breaker runtime wiring：NOT
PROVEN）。
