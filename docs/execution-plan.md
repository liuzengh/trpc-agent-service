# 开发执行清单

## 交付收尾（不 push）

1. 当前实现已整理成本地提交 `c5104ca`，私有凭据/备份排除，陈旧文档状态已校准。
2. 新增两个租户、两个真实 Worker 的隔离联合测试，覆盖共享 Session/Memory/Knowledge、工具权限、处理中故障接管与去重；旧多进程脚本已替换为安全入口。
3. 手动 Agent 启停增加工作区锁、PID 身份记录、pidfd 停止和就绪等待；新增 `status.sh` 统一只读状态检查，不设置开机自启。
4. 完整隔离回归已通过，本轮收尾代码与记录纳入第二个本地提交，不 push；生产集群、真实通知接收方、云后端和大规模质量/容量仍按环境另行确认。

证据与验证边界见[收尾记录](validation/delivery-closeout-2026-09-08.md)。

## 2026-09-08 当前进度

只读项目文档 MCP 已在第三次 `fencing` 查询中通过同请求的工具调用、完整 trace 和 Telegram 单次送达/回执验证，见[记录](validation/project-docs-mcp-2026-09-08.md)。前两次未完整通过的记录保留。这不是外部企业业务 MCP 接入。

rc.7 的安全发送诊断已补齐并部署：HTTP 错误分类与阶段、Outbox 错误信息、审计元数据、失败 trace 状态和隐私边界测试。未知发送仍不自动重发，旧失败记录不修改，见[记录](validation/delivery-diagnostics-2026-09-08.md)。

用户已提供专用 Embedding 配置并确认采用 1024 维。rc.8 已部署，检查器与运行时共用安全 Embedder；已配置独立 Qdrant Key/目标集合，经持久 Job 导入公开合成说明，并通过真实问题检索、作用域过滤和 Qdrant/Agent 重启后读取。App version 7 / `tutorial-knowledge-v7` 已发布，原测试私聊的真实 knowledge_search、Embedding/检索 trace 和 Telegram 单次送达均已确认；原数据卷、聊天、附件和记忆保留，见[本地记录](validation/knowledge-qdrant-2026-09-08.md)。

本地工作项写操作已通过真实链路：rc.9 / `tutorial-workitem-v8`，审批前业务记录为 0，用户批准后实际创建一条；重复批准仅产生平台提示，没有新增模型/工具执行或工作项。工具/业务记录、两类回执与跨审批 trace link 已核对，每工具用户白名单和仅私聊限制保留，见[记录](validation/workitem-approval-2026-09-08.md)。这不是外部工单系统接入，本组测试无需继续重复发送。

## 2026-09-07 后续开发

五项可靠性修复、Agent MCP 和最小附件链路已完成代码与自动/隔离测试，详见[后续补齐记录](reliability-followup.md)。随后本地已升级 rc.5/schema 23；Telegram 文本附件、MinIO 跨重启读取、PostgreSQL 长期记忆保存及重启后工具读取均已核对真实记录。见[附件](validation/artifact-minio-2026-09-07.md)和[记忆](validation/memory-postgres-2026-09-07.md)。下方是前一阶段历史，不代表最新运行状态。

## 前一阶段记录（历史，不是当前运行状态）

当前基线：核心平台、Telegram 和企业微信群文本的真实 Agent 链路已跑通。以下按用户重新确认的七项清单收尾，不重复基础聊天测试。

1. **已提交**：企业微信接入、真实验证记录与隔离备份修复，提交 `aabbadd`。
2. **代码与隔离测试已完成**：SQL/Redis 分角色权限生成器、按角色初始化依赖、轮询锁键空间分离、网络模板收窄。没有替换日常账号或应用真实集群策略，见[部署权限](deployment-permissions.md)。
3. **代码、隔离测试及图片真实验证已完成**：图片不阻塞文本，rc.2 新图片经重叠读取后隔离/审计各 1 条；整页失败保留进度、版本约束的检查点恢复与原子审计有自动和数据库测试，没有清空既有去重记录，见[恢复说明](channel-recovery.md)。
4. **代码、隔离测试及本地启用已完成**：SQL 聚合积压、轮询失败、未知发送、采集失败/过期指标与离线验证的告警规则，见[监控说明](monitoring.md)。本地监控容器已更新启动；真实通知接收方后置。
5. **代码与隔离测试已完成**：Worker 取消接管、完成确认丢失、故障退避，独立 PostgreSQL 暂停/业务 dump 恢复，以及现有并发回归；真实容量边界已明确。见[恢复记录](validation/recovery-2026-09-06.md)和[容量说明](capacity.md)。
6. **已完成**：[统一运行手册](operations-runbook.md)涵盖安装、配置、手动启停、升级与排障；README 已区分企业微信 MCP 真实收发与自建应用回调模拟测试。
7. **本地开发与回归已完成并提交**：rc.1 完成全仓库 race/lint/build、独立 SQL/Redis 合约与备份、告警规则、文档链接和候选镜像非 root 启停；随后 rc.2 媒体修复通过全仓库 race/lint、真实窗口复验和本地启用。rc.2 新图片持久隔离去重也已真实验证。原[候选版本记录](validation/release-candidate-2026-09-06.md)与后续[媒体记录](validation/wecom-media-2026-09-06.md)分开保留；未推送或发布远端版本。

该阶段未包含管理页面、Agent MCP、多种编排和完整多媒体。后续 Agent MCP 与限类型附件进展见上方 rc.4 记录，其余不计入本次完成范围。已通过的真实 Telegram/企业微信基础链路保留证据。

**当时运行**：本地开发实例为 `0.2.0-rc.2`、schema 16；原启用过程见[记录](validation/rc1-rollout-2026-09-06.md)。

**当时验证**：用户确认文字只收到一条回复；rc.2 新图片经完整重叠窗口后只有一条隔离记录和审计记录，未进入 Agent 或生成回复。图片 `media_id` 变化导致的重复隔离已修复，见[媒体记录](validation/wecom-media-2026-09-06.md)。

**本轮无需继续发测试消息**。本地开发及这条 IM 链路的收尾已完成；真实生产账号/集群部署、实际告警通知和生产容量仍需环境与配置配合，不包含在上述完成结论内。其他媒体类型、真实分页、多租户高负载和云后端验证按实际部署范围另安排，不将本次单群验证扩大解释。
