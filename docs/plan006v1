# P0-06 Redis Claim、Lease、Fencing 与限流实施计划

## 1. 当前代码状态

- P0-01 至 P0-05 已完成，当前仓库已有：
  - `tenant.TenantContext`、租户/Binding 校验和稳定的多租户领域模型。
  - `storage` 层的 Claim、Lease、fencing、Outbox 等契约与内存 Fake 实现。
  - PostgreSQL P0-05 初始 migration，已有 `message_dedup` 表，包含租户限定的消息键、状态、owner、attempt、`fence_token`、过期时间和响应引用字段。
  - PostgreSQL migration runner、独立事务、checksum、测试库集成测试和显式测试 DSN 约束。
- 当前没有 `trpcservice/storage/redis/`、`trpcservice/ratelimit/` 或 Redis client 实现，也没有 Redis dependency。
- 当前 `platform` 和内存存储中已有 100 并发 Claim、Claim 过期接管、旧 fencing token 拒绝等行为测试，但这些不覆盖真实 Redis 原子性、进程间并发、Redis 故障或 PostgreSQL fallback。
- 当前没有 PostgreSQL Session lease 表；P0-05 的 `session` 表本身不能安全承载 lease owner、过期时间和 fencing token，因此 Claim+Lease fallback 需要新增一条独立 migration。
- 当前没有 Agent、IM 或模型执行流程需要被 P0-06 修改；P0-06 只提供存储/协调组件和测试，不接入后续 Worker/Gateway。

## 2. 需求与现有代码的差距

- 缺少基于 Redis Lua script 的原子 Claim：需要保证 100 个并发请求跨 goroutine、连接和进程只有一个 owner 成功取得当前 claim。
- 缺少 Claim lease：同一 dedup key 在 lease 未过期时不能被其他 owner 接管，过期后必须原子接管并增加 attempt/fencing token。
- 缺少独立 Session lease：Session 只能同时由一个 worker 持有，Redis TTL 到期后允许新 owner 接管。
- 缺少单调 fencing token 和带 token 校验的 commit/renew/release，旧 owner 即使恢复也不能提交结果或续租。
- 缺少租户、Binding、Chat 三个维度的原子限流；限流 key 必须包含 `tenant_id`，不能使用全局业务 key。
- 缺少 Redis 故障策略。本计划采用已确认的 `Claim+Lease` PostgreSQL fallback：Claim 和 Session lease 在 Redis 不可用时切换到 PostgreSQL；限流在 Redis 不可用时 fail closed，不尝试用 PostgreSQL 模拟高吞吐限流。
- 缺少 Redis integration test、故障注入测试和跨租户 key 隔离测试。

## 3. 详细实施步骤

### 3.1 依赖与配置

1. 在 `go.mod`/`go.sum` 中加入并锁定 `github.com/redis/go-redis/v9` 的明确版本，使用现有 Go 版本兼容的稳定版本；不引入第二套 Redis client。
2. 新增 Redis 配置：URL/地址、密码由调用方传入但不出现在错误中，DB、连接池大小、Dial/Read/Write timeout、Claim TTL、Session lease TTL、renew interval、限流窗口和故障策略。
3. 对配置做严格校验：TTL/interval 必须为正，renew interval 必须小于 lease TTL，限流阈值不能为负，Redis key prefix 必须非空且不能包含 key 分隔符。
4. 提供显式 `Close`，由创建 Redis client 的组件负责关闭；fallback 注入的 PostgreSQL backend 生命周期由调用方负责。

### 3.2 Redis key 与 Lua 原子操作

1. 所有 key 通过结构化 builder 生成，格式固定为带版本的租户限定 key，例如：
   - `trpc:v1:dedup:{tenant}:{channel}:{binding}:{external_message_id}`
   - `trpc:v1:session-lease:{tenant}:{session_id}`
   - `trpc:v1:fence:{tenant}:{resource_type}:{resource_id}`
   - `trpc:v1:rate:{tenant}:{dimension}:{binding_or_chat}:{window}`
2. 对用户可控字段做长度、空值、控制字符和分隔符校验；不允许调用方通过字段注入额外 key 段。
3. 使用 `go:embed` 或受信任 `fs.FS` 保存 Lua scripts，不在运行时拼接 Lua 代码；每个 script 明确声明 KEYS/ARGV 数量。
4. Claim script 在一次 Redis EVAL 中完成：
   - 新 key：创建 hash，写入 status/owner/attempt/fence/expiry，设置 TTL，返回 acquired 和 fencing token。
   - 当前 owner 且 lease 未过期：返回 already-owned，不能重复增加 attempt/token。
   - 已完成：返回 completed 和 response reference，调用方不得重新执行 Agent。
   - 已过期：校验旧记录，原子替换 owner、增加 attempt、生成单调 fencing token、刷新 TTL，返回 takeover。
5. Complete/Fail/Renew/Release scripts 必须同时比较 owner 和 fencing token；不匹配或已过期统一返回 `ErrFenceRejected`/`ErrLeaseLost`，不得修改 Redis 状态。
6. Session lease 使用 hash + TTL；每次 takeover 通过 `INCR` 生成单调 fencing token，并将新 token 写入 lease。TTL 只负责失效，安全性依赖每次写操作的 token compare。
7. Redis script 返回明确的整数/字符串结果码，由 Go 层解析为稳定错误，不直接暴露 Redis 原始错误给业务调用方。

### 3.3 Claim、Session lease 与续租 Context

1. 实现 `RedisIdempotencyRepository`：`Claim`、`Complete`、`Fail`，保持与现有 storage 契约的 tenant/context 校验和 Claim 状态语义一致。
2. 实现 `RedisLeaseManager`：`Acquire`、`Renew`、`Release`、`Validate`，同时支持 Claim lease 和 Session lease 的资源类型隔离。
3. 提供 `RunWithLease(ctx, lease, fn)` 或等价 helper：
   - 获得 lease 后启动单一 renewal goroutine。
   - 按 renew interval 续租，并使用父 Context 派生 execution Context。
   - 任意续租失败、Redis 连接错误、owner/token 不匹配或 lease 过期，立即 cancel execution Context，并返回 `ErrLeaseLost`。
   - `fn` 返回后停止 renewal goroutine，再执行带 token 校验的 release；避免 goroutine、timer、channel 泄漏。
4. renewal 失败后禁止继续调用持久化 commit API；调用方必须看到可区分的 `ErrLeaseLost`，而非被吞掉的普通 Redis error。
5. 释放和过期接管必须幂等；旧 owner 的 release 不能删除新 owner 的 lease。

### 3.4 PostgreSQL fallback

1. 新增 `migrations/000002_redis_fallback.up.sql` 和配对 down migration，新增租户限定的 `session_lease` 表：
   - `(tenant_id, session_id)` 主键。
   - owner、fencing token、leased_until、updated_at 字段。
   - `fencing_token >= 0`、`leased_until` 非空等 CHECK。
   - `(tenant_id, session_id)` 外键引用 `session`。
   - 必要的 tenant/session/expiry 索引。
2. `message_dedup` 复用 P0-05 已有字段，不重复创建 Claim 表；增加 P0-06 migration 对现有列/约束的校验或仅在缺失时 fail closed，不能用不兼容 `IF NOT EXISTS` 掩盖 Schema 漂移。
3. 新增抽象 fallback backend，提供：
   - PostgreSQL Claim：事务内使用 `INSERT ... ON CONFLICT` 和 `SELECT ... FOR UPDATE`，按过期时间原子接管，attempt/fencing token 单调增加。
   - PostgreSQL Complete/Fail：事务内比较 owner 和 fencing token；旧 token 拒绝提交。
   - PostgreSQL Session lease Acquire/Renew/Release：事务内锁定 `(tenant_id, session_id)` 行，过期才能接管，所有写入都比较 fencing token。
4. Redis facade 只在明确的 Redis connectivity/timeout 类错误时 fallback；业务校验错误、fence rejection、脚本协议错误不能 fallback，否则会掩盖逻辑 bug。
5. Claim/Lease 一旦选择 PostgreSQL fallback，单次操作不能在 Redis 和 PostgreSQL 间重复写入；返回结果中标记 backend，便于 metrics/audit，但不修改 Agent/IM/model 类型。
6. Redis 恢复后不自动复制或双写 lease 状态；新的请求重新使用 Redis，正在 PostgreSQL fallback 上持有的 lease 必须按原 backend 完成或过期，避免两个 backend 同时授予同一 Session。
7. 限流不做 PostgreSQL fallback：Redis 不可用时返回明确的 `ErrRateLimiterUnavailable`，由上层选择拒绝或受控降级；P0-06 不修改 Gateway/Worker，因此不决定 HTTP/IM 响应格式。

### 3.5 三维限流

1. 实现 `RateLimiter.Allow(ctx, tenantID, bindingID, chatID, rule)`。
2. 使用固定窗口 Lua script，在一次 EVAL 中递增并设置窗口 TTL，原子返回 allowed、remaining、reset time。
3. 分别维护 tenant、binding 和 chat 三个 key；一次 Allow 同时检查三维额度，任一超限则整体拒绝，避免只限制局部维度。
4. key 必须包含 `tenant_id`，Binding 和 Chat 不能跨租户共享计数器；窗口边界使用 Redis server time，避免依赖 worker 本地时钟。
5. 明确定义规则：窗口长度和最大次数为正值，chat 维度要求 chat ID 非空；无 chat 的私聊使用稳定的外部用户/会话维度，不使用空字段造成全租户共享。

### 3.6 测试与文档

1. 保留现有 Fake storage contract tests，不把 Fake 当作 Redis 原子性证明。
2. 增加 Redis 单元测试：key 编码、输入校验、Lua 返回码解析、配置校验、错误映射和 Context cancel。
3. 增加 Redis integration tests，使用显式 `TEST_REDIS_URL`，没有环境变量时跳过；不得猜测默认 Redis 或连接生产实例。
4. 增加 PostgreSQL fallback integration tests，继续使用显式 `TEST_DATABASE_URL` 和隔离 schema/数据库。
5. 更新 P0-06 README，说明 key schema、TTL/fencing 语义、fallback 选择、限流 fail-closed 和生产 Down 边界。

## 4. Go 接口和数据结构

建议新增接口保持基础设施类型独立，不暴露 Redis client 类型：

```go
type ClaimStatus string

const (
    ClaimAcquired  ClaimStatus = "acquired"
    ClaimInFlight  ClaimStatus = "in_flight"
    ClaimCompleted ClaimStatus = "completed"
)

type DedupKey struct {
    TenantID          string
    Channel           string
    BindingID         string
    ExternalMessageID string
}

type Claim struct {
    Key         DedupKey
    Status      ClaimStatus
    OwnerID     string
    Attempt     int
    FenceToken  uint64
    ResponseRef string
    ClaimedAt   time.Time
    ExpiresAt   time.Time
    Backend     BackendKind
}

type ClaimRepository interface {
    Claim(context.Context, tenant.TenantContext, string, time.Duration) (Claim, error)
    Complete(context.Context, tenant.TenantContext, DedupKey, string, uint64) error
    Fail(context.Context, tenant.TenantContext, DedupKey, uint64, bool) error
}

type LeaseKind string

const (
    LeaseSession LeaseKind = "session"
    LeaseClaim   LeaseKind = "claim"
)

type Lease struct {
    Kind         LeaseKind
    TenantID     string
    ResourceID   string
    OwnerID      string
    FenceToken   uint64
    ExpiresAt    time.Time
    Backend      BackendKind
}

type LeaseManager interface {
    Acquire(context.Context, tenant.TenantContext, LeaseKind, string, string, time.Duration) (Lease, error)
    Renew(context.Context, Lease, time.Duration) (Lease, error)
    Release(context.Context, Lease) error
}

type RateLimiter interface {
    Allow(context.Context, RateLimitRequest) (RateLimitResult, error)
}

type RateLimitRequest struct {
    TenantID  string
    BindingID string
    ChatID    string
    Rule      RateLimitRule
}

type RateLimitRule struct {
    Window time.Duration
    TenantLimit  int64
    BindingLimit int64
    ChatLimit    int64
}

type RateLimitResult struct {
    Allowed       bool
    Remaining     map[string]int64
    ResetAt       time.Time
}
```

Additional implementation types:

- `RedisBackend` containing `redis.UniversalClient`, key builder, script SHA/cache and timeout config.
- `FallbackCoordinator` containing Redis primary plus PostgreSQL Claim/Lease backend and explicit fallback policy.
- `LeaseContext`/`LeaseRunner` containing derived Context, cancel function, renewal error channel and shutdown state。
- `BackendKind` values `redis` and `postgres` for diagnostics only; this must not alter Agent/IM/model behavior。
- Sentinel errors: `ErrAlreadyClaimed`, `ErrLeaseLost`, `ErrFenceRejected`, `ErrRateLimitExceeded`, `ErrRateLimiterUnavailable`, `ErrRedisUnavailable`, `ErrInvalidKey`；复用现有 `storage` sentinel errors，避免重复定义不兼容错误。

## 5. 测试用例

### Claim 与 fencing

- 100 个 goroutine 对同一 `(tenant, channel, binding, external_message_id)` Claim，断言恰好一个 `ClaimAcquired`/takeover winner。
- 不同 tenant 使用相同 channel、Binding 和 external message ID，断言分别成功且 key 不冲突。
- 未过期 Claim 被其他 owner 请求，断言不能接管。
- 等待 TTL 过期后第二 owner 接管，断言 attempt 增加、fencing token 严格递增。
- 完成后重复 Claim 返回 `ClaimCompleted` 和相同 response reference，不重新产生执行权。
- 旧 fencing token 执行 Complete、Fail、Renew、Release 全部被拒绝，且不能修改新 owner 数据。
- Redis EVAL 返回未知结果码、参数非法、Context 取消时不改变状态。

### Session lease 与 Context

- 100 个并发 Session Acquire，断言只有一个成功。
- lease 未过期时其他 owner 不能 Acquire。
- lease 过期后新 owner 成功接管，旧 owner token 不能续租或释放。
- renewal 正常运行时 execution Context 保持可用。
- Redis 续租返回 timeout、连接断开、脚本返回 fence rejection 时，execution Context 在有界时间内取消，并返回 `ErrLeaseLost`。
- lease runner 返回后 renewal goroutine 和 timer 均停止；使用 `go test -race` 和必要的 goroutine 计数/重复运行检查。
- Release 幂等；旧 owner Release 不删除新 owner 的 lease。

### PostgreSQL fallback

- Redis 不可达时 Claim fallback 成功，100 并发仍只有一个 winner。
- Redis 不可达时 Session lease fallback 成功，100 并发仍只有一个 owner。
- PostgreSQL lease 过期后可接管，旧 token 不能提交结果。
- PostgreSQL transaction 中途失败不留下半更新的 owner/token/expiry。
- Redis 业务错误不触发 fallback；只有连接/timeout 类错误触发 fallback。
- Redis 恢复后不发生 Redis/PostgreSQL 双重 lease；fallback lease 在原 backend 完成或过期。
- PostgreSQL fallback 使用不同 tenant 的相同资源 ID 时互不影响。

### 限流

- tenant、Binding、Chat 三个维度分别达到上限时拒绝。
- 同一窗口内并发请求不超过配置额度，窗口过期后恢复。
- 相同 Binding/Chat ID 在不同 tenant 下计数器隔离。
- Redis server time 决定窗口，不受本地 worker 时钟偏差影响。
- Redis 不可用时返回 `ErrRateLimiterUnavailable`，不静默放行。

## 6. 验收命令

本地只使用显式测试服务：

```bash
# 仅设置本地/CI 测试 Redis，不使用默认或生产 DSN
export TEST_REDIS_URL='redis://127.0.0.1:6379/15'
export TEST_DATABASE_URL='postgres://...@127.0.0.1:5432/trpc_agent_test?sslmode=disable'

# 格式、全量测试和静态检查
gofmt -w trpcservice/storage/redis trpcservice/ratelimit trpcservice/storage/postgres
go test ./... -count=1
go test ./trpcservice/storage/redis ./trpcservice/ratelimit -race -count=1
go vet ./...

# Redis integration / fallback integration，脚本必须继续要求显式测试 DSN
TEST_REDIS_URL="$TEST_REDIS_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" \
  ./scripts/test-redis-components.sh
```

验收必须额外确认：

- `go test -race` 覆盖 Redis package、rate limit package 和 fallback coordinator。
- 无 `TEST_REDIS_URL` 时 integration test 明确跳过或脚本以非零状态拒绝，不尝试默认 Redis。
- 无 `TEST_DATABASE_URL` 时 fallback integration test 不连接任何默认/生产 PostgreSQL。
- P0-06 package 不 import Agent、IM adapter、模型 provider 或执行流程包。
- migration `Up`、重复 `Up`、checksum mismatch、测试环境 `Down` 和重新 `Up` 全部通过。

## 7. 风险和回滚方案

- **Redis 脚本原子性错误**：所有状态转换集中在 Lua，并使用 100 并发和 takeover 测试；发现问题时停止启用 Redis backend，保留 PostgreSQL fallback。
- **Redis 连接分区**：lease renewal 失败必须 cancel Context；不得在无法确认 lease 时继续提交结果。Redis 恢复期间由 fallback coordinator 选择单一 backend，避免双写。
- **fencing token 回绕或非单调**：使用 Redis `INCR`/PostgreSQL `bigint`，校验非负和溢出错误；发现溢出时拒绝发放新 lease，不能回到 0。
- **fallback 性能下降**：只在 Redis 故障时使用 PostgreSQL；监控 fallback 次数、延迟和数据库锁等待，恢复 Redis 后切回。
- **PostgreSQL migration 风险**：`000002` 与 down 文件成对、独立事务、checksum 校验；只在隔离测试库执行 down。生产采用 forward migration/备份恢复，不把 down 暴露为生产入口。
- **限流 fail-closed 造成短时拒绝**：明确返回可观测错误并记录指标；不在 P0-06 中修改 Gateway/IM 响应逻辑，由后续任务决定用户侧表现。
- **回滚**：移除 Redis backend 注册，切换到 PostgreSQL Claim+Lease fallback；保留 `message_dedup` 和 `session_lease` 数据；如需回滚 Schema，只在隔离测试库执行 `000002` down，禁止删除 P0-05 初始业务表。

## 8. 计划修改的具体文件

### 新增

- `trpcservice/storage/redis/config.go`：Redis 配置、默认值和校验。
- `trpcservice/storage/redis/errors.go`：Redis/fallback/lease 错误映射。
- `trpcservice/storage/redis/keys.go`：版本化多租户 key builder 和字段校验。
- `trpcservice/storage/redis/scripts.go`：Claim、lease、fencing、限流 Lua script。
- `trpcservice/storage/redis/claim.go`：`RedisIdempotencyRepository`。
- `trpcservice/storage/redis/lease.go`：Session/Claim lease、renew、release 和 fencing 校验。
- `trpcservice/storage/redis/ratelimit.go`：Redis 三维原子限流实现。
- `trpcservice/storage/redis/fallback.go`：Redis 故障分类与 PostgreSQL fallback coordinator。
- `trpcservice/storage/redis/config_test.go`、`keys_test.go`、`claim_test.go`、`lease_test.go`、`ratelimit_test.go`、`fallback_test.go`：单元和 fake backend 测试。
- `trpcservice/ratelimit/ratelimit.go`：与 Redis client 解耦的 `RateLimiter` 公共接口和数据结构。
- `trpcservice/ratelimit/ratelimit_test.go`：限流契约测试。
- `trpcservice/storage/postgres/dedup.go`：PostgreSQL Claim fallback。
- `trpcservice/storage/postgres/lease.go`：PostgreSQL Session lease fallback。
- `trpcservice/storage/postgres/dedup_test.go`、`lease_test.go`、`fallback_integration_test.go`：fallback 测试。
- `migrations/000002_redis_fallback.up.sql`：`session_lease` 表和索引。
- `migrations/000002_redis_fallback.down.sql`：仅测试库使用的反向 migration。
- `scripts/test-redis-components.sh`：显式 `TEST_REDIS_URL`/`TEST_DATABASE_URL` 校验和集成测试入口。
- `trpcservice/storage/redis/testdata/*.lua` 或等价的嵌入脚本资源：Redis 原子操作脚本。

### 允许修改

- `go.mod`、`go.sum`：仅加入 Redis client 及 P0-06 直接需要的依赖。
- `trpcservice/storage/repository.go`、`trpcservice/storage/errors.go`：仅在现有 Claim/Lease 契约无法表达 Redis 实现所需的 backend、renew 或 fencing 语义时做最小兼容扩展；不修改 Agent、IM、模型逻辑。
- `trpcservice/storage/postgres/`：仅新增 P0-06 的 fallback repository 和必要测试，不改动既有 P0-05 migration runner 行为。
- `migrations/`、`scripts/`：仅新增 P0-06 migration、测试入口和文档。

### 明确不修改

- `trpcservice/agent/`、模型 provider、Runner 和 Tool 执行逻辑。
- `trpcservice/channels/`、Web、企业微信、Telegram 或其他 IM adapter 行为。
- `trpcservice/gateway/`、`trpcservice/worker/`、Outbox dispatcher 和后续 P0-07/P0-08/P0-09 业务流程。
- 生产服务器、生产数据库、生产日志、密钥、Token、密码、`data/` 运行时文件。
- 不执行 git commit、git push，不引入 Redis 集群部署、监控平台或后续任务基础设施。

## 默认决策与验收标准

- Redis Lua + Redis server time 是主路径的原子性和时间来源。
- Redis 故障时 Claim 和 Session lease 均 fallback 到 PostgreSQL；限流 fail closed，不用 PostgreSQL 替代限流。
- fencing token 是每次 acquire/takeover 单调递增的资源版本；所有完成、失败、续租和释放都必须带 owner 与 token compare。
- lease renewal 失败必须在有界时间内取消执行 Context，旧 worker 无论是否恢复都不能提交结果。
- P0-06 不修改 Agent、IM 和模型逻辑，只提供可供后续 Worker/Gateway 使用的基础设施接口。
- 最终验收以 100 并发单赢家、过期接管、旧 token 拒绝、续租失败 cancel、三维限流隔离、Redis 故障 Claim+Lease fallback 和 `go test -race` 全部通过为准。
