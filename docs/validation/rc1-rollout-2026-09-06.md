# 0.2.0-rc.1 本地启用记录

日期：2026-09-06，时区 Asia/Shanghai。用户确认“模型已启动”后，继续备份、迁移和启用候选版本，等待原测试群的真实媒体/文本验证。

## 已执行

- 模型 `glm-5.3-flash` 预检成功，返回 `OK`，耗时约 3.03 秒。
- 原 PostgreSQL/Redis 容器处于停止状态。按 Compose 新配置启动后仍使用原命名数据卷，端口限制为 `127.0.0.1:5432/6379`；没有清卷或新建空库。
- 升级前 schema 为 14；1544 个 Run 和 Outbound 均已完成/发送，3078 个后台任务均完成，没有待执行或待发送任务。
- 原 `.env`、已有候选二进制、旧日志/PID、PostgreSQL 自定义格式 dump、Redis RDB 保存在 `data/rc1-upgrade.qTQQWm97/`，目录 `0700`，配置和数据库快照 `0600`。SQL dump 的归档目录可读取，Redis 容器/本地副本 SHA256 一致；容器内临时 RDB 副本已清理，原数据和本地备份保留。候选二进制备份不是上一运行版本的回退二进制。
- 19:34:38 完成 migration 015/016，未重新 bootstrap 或修改任何 Binding。
- 启动 `0.2.0-rc.1`、role=all、真实模型、Redis Session/Coordinator/Idempotency/Queue，以及原 OTel 监控栈；`/readyz` 返回 ready。
- 13 条 Prometheus 规则加载健康，`agent_backlog_snapshot_up=1` 且快照时间在更新。未配置真实告警通知接收方。

本次没有修改 `.env`、启用其他群/用户、重新设置 Telegram Webhook 或启动 Tunnel。Telegram Binding 仍为 active/version 2，MCP Binding 仍为 active/version 1、原 1 个群/1 个人类账号。分角色生产 SQL/Redis 账号未应用，当前仍是开发环境的 all 模式。

## 检查点补查与真实测试边界

升级前 MCP 检查点为 `2026-09-06T10:06:37Z`（18:06:37）、版本 456。停机期间积累了约一个半小时的时间窗口，不能静默跳过。

启动后检查点正常推进：19:37:29 检查时已到 18:18:37、版本 468，落后约 4733 秒；启动以来未出现该 Binding 的 `channel_poll_failed` 审计。当前每 10 秒最多前进 60 秒，因此追赶净速度约为现实时间的 5 倍，估计仍需约 15–16 分钟，实际取决于上游延迟与错误。

没有调用检查点恢复/跳过接口，没有清除 seen 或发送尝试。追赶时发现的已处理记录仍按原指纹去重。

下一步仍需用户在原授权测试群发一张不含敏感信息的图片，再 @机器人发送带新标记的普通文本。图片应隔离，不下载、不分析；正常文本应进入 Agent 并回复一次。**本记录只证明版本已启用、依赖与轮询正常，不把尚未发送的真实媒体测试记为通过。** 补查期间发送的新消息会等待轮询追上其时间窗口。
