# P0-07 真实 tRPC-Agent-Go Runtime Adapter 计划

## 项目背景与全新上下文导航

### 1. 项目是什么

本仓库是一个基于 **tRPC-Agent-Go** 的多租户、节点化 Agent 部署平台实战项目。目标不是实现一个单租户聊天 Demo，而是建立一条能够指导后续工程落地的生产级 Agent 平台链路：多个租户可以拥有不同的 Agent 应用、模型配置、工具权限、IM 绑定和数据后端；多个无状态 Worker 可以共同处理消息；Session、Memory、Summary、Audit 和 Outbox 等状态由共享后端保存；外部消息按照至少一次投递处理，并通过 Claim、Lease、epoch 和 fencing 防止重复执行、并发覆盖和旧节点提交。

项目题目、业务背景、交付物、验收标准和示范目录见 [`README.md`](../README.md)。其中最重要的产品要求包括：多租户隔离、企业微信/Telegram 等 IM 接入、多后端数据同步、Runner/Tool/Guardrail 治理、审计与可观测性、故障恢复和生产部署。P0-07 只负责把“真实 Agent 执行”接入现有平台边界，不负责一次性实现这些后续能力。

### 2. 哪些文档是权威依据

全新上下文的 Pi 必须先按以下顺序阅读，不要只根据本文件或旧会话推断项目状态：

1. [`ARCHITECTURE.md`](ARCHITECTURE.md)：目标架构和当前边界的权威说明。重点阅读 Worker 无状态、PostgreSQL 事实源、Redis 协调、TenantContext、Session/Lease/fencing、Runner Adapter、事件链路、IM Adapter 和禁止事项。
2. [`implementation-plan.md`](implementation-plan.md)：从 Git `62d6963` 基线继续执行的总计划。重点阅读固定决策、P0-01 至 P0-06 的已完成边界、P0-07/P0-08 的依赖关系、总体验收和完成定义。
3. [`project-status.md`](project-status.md)：当前代码事实核对。它优先回答“代码现在有什么”，不能用接口存在、迁移存在或普通测试通过推断生产能力已完成。
4. [`README.md`](../README.md)：原始项目要求、验收标准、运行方式和代码目录。
5. [`p0-06-acceptance-matrix.md`](p0-06-acceptance-matrix.md)：P0-06 的阶段验收证据和实际测试边界；阅读它来了解基础层已验证到什么程度。
6. [`plan006v2.md`](plan006v2.md)：P0-06 的技术实施计划，属于历史过程材料，不替代总计划和当前状态。
7. [`plan006v2困境分析.txt`](plan006v2困境分析.txt)、[`plan006v2方案修正与困境解决.txt`](plan006v2方案修正与困境解决.txt)、[`plan006v2困境解决的实际操作范式.txt`](plan006v2困境解决的实际操作范式.txt)：上一阶段范围膨胀、重复对话和过度审查的原因、修正方案及执行范式。它们解释了本计划为什么采用 P0-07A 至 P0-07D 的单阶段关闭规则。

文档冲突时，按以下优先级判断：实际代码和测试结果 > `project-status.md` 的状态核对 > `ARCHITECTURE.md` 的架构边界 > `implementation-plan.md` 的任务顺序 > 本文件的执行细节 > 历史 `plan006*` 过程材料。若发现基础契约不足，不得静默扩大 P0-07；先记录为当前阶段唯一阻塞项或 backlog。

### 3. 当前项目起点

P0-01 至 P0-06 已被总计划标记为基础层完成，但这不等于首个生产版本完成。当前起点可以准确描述为：

- 已有 Go 服务入口、最小 HTTP 路由、租户/Agent/Channel Binding/Session/Event/Memory/Summary/Artifact/Audit 领域模型和基础测试。
- 已有 PostgreSQL 迁移器、checksum、readiness、事实源 schema，以及 Redis/PostgreSQL Claim、Session Lease、epoch/fencing、故障切换和限流基础设施。
- 主进程仍是同步演示链路：`MemoryStore -> platform.Runner -> EchoResponder`。设置 `DATABASE_URL` 目前主要用于迁移/readiness，不代表业务 Repository 已切换到 PostgreSQL。
- 企业微信和 Telegram 当前只是最小协议占位；尚未实现真实验签、解密、消息去重、异步发送或生产 IM API。
- 尚未实现真正的 tRPC-Agent-Go Runner Adapter、异步 Job Queue、Gateway/Worker 分离、Outbox Dispatcher、真实业务 Repository、API 鉴权、OTel、向量库和对象存储。
- 因此 P0-07 的真实起点不是“修改一个已经存在的 Runner”，而是从一个只有 Echo 联调链路和 Agent 占位包的仓库，建立可测试的 Runtime 适配边界。

P0-06 的 Claim/Lease/fencing 语义已经是后续执行层的基础契约。P0-07 不重新实现、不重审、不修改 P0-06 的协调、迁移和租户领域逻辑；P0-08 才负责把 Runtime 纳入 Session Execution Orchestrator，P0-09 负责业务 Repository，P0-10/P0-11 负责 Job、Gateway、Worker 和 Outbox。

### 4. 目标架构中 P0-07 所处的位置

目标消息链路是：

```text
Web Chat / 企业微信 / Telegram
        -> Gateway / Channel Adapter
        -> Claim + Job Queue
        -> Agent Worker
        -> P0-07 Agent Runtime Adapter
        -> tRPC-Agent-Go Runner / Model / Tool / Guardrail
        -> 平台 RunnerEvent / AgentResult
        -> Session Event + Audit + Outbox
        -> IM Sender / Web Response
```

P0-07 只实现中间的 Runtime 边界和当前同步 `platform` 兼容桥接。它必须接收已经验证的 `TenantContext`，将平台自己的 `AgentInput` 转换成 tRPC-Agent-Go 所需的内部请求，将框架事件转换成平台自己的 `RunnerEvent`，并正确传播 `context.Context`、错误和取消。框架类型只能存在于 `trpcservice/agent` 的未导出实现中，不能进入 `tenant`、`channels`、`storage`、`platform` 的公共接口。

P0-07 不拥有 Session、Memory、数据库事务、Claim、Lease、Job、Outbox、IM 回调或服务级 shutdown。Runtime 是一次执行的无状态适配层：它可以使用传入的只读 Agent 配置和历史消息，但不能把 Session 或租户可变配置缓存为事实源，也不能让模型或 Tool 修改租户配置。

### 5. P0-07 相关代码位置

新上下文 Pi 开始编码前，至少检查以下文件和目录的实际内容：

| 位置 | 当前职责 | P0-07 使用方式 |
| --- | --- | --- |
| [`trpcservice/agent/agent.go`](../trpcservice/agent/agent.go) | Agent 包占位；当前没有完整 Factory/Runtime | P0-07A/B 的主要实现边界；框架依赖只能封装在这里或其子文件 |
| [`trpcservice/platform/platform.go`](../trpcservice/platform/platform.go) | 同步 `platform.Runner`、`Responder`、`EchoResponder`、内存联调链路 | P0-07C 增加 Runtime bridge；保留 Echo 行为和既有同步形状 |
| [`trpcservice/platform/platform_test.go`](../trpcservice/platform/platform_test.go) | Echo Runner 的幂等、错误和租户隔离测试 | P0-07C 增加 bridge/模式选择回归，不重写既有测试语义 |
| [`cmd/trpc-service/main.go`](../cmd/trpc-service/main.go) | 当前固定注入 `EchoResponder` 并启动 HTTP 服务 | P0-07C 增加显式 `MODEL_PROVIDER=runner|echo` 选择；runner 初始化失败不得静默降级 |
| [`trpcservice/tool/tool.go`](../trpcservice/tool/tool.go) | Tool 占位包 | 只有 Runtime 需要纯数据 `ToolInvoker` 边界时做最小接口修改，不实现治理/审批/危险工具 |
| `trpcservice/tenant/` | Tenant、TenantContext、权限和校验 | 只读并复用现有校验；不修改生产租户规则来迁就测试 |
| `trpcservice/session/`、`trpcservice/memory/`、`trpcservice/audit/` | Session、消息事件、Memory 和 Audit 领域类型 | 只做平台类型转换；不在 P0-07 实现 Repository 或执行编排 |
| `trpcservice/storage/`、`migrations/` | 存储契约、Claim/Lease/Outbox 契约和数据库迁移 | P0-07 不修改、不接入业务存储 |
| `trpcservice/channels/`、`trpcservice/web/` | 当前最小 Channel/HTTP/Webhook 壳 | P0-07 不实现真实 IM 验签、鉴权或 Gateway |
| [`go.mod`](../go.mod)、[`go.sum`](../go.sum) | Go 依赖；tRPC-Agent-Go `v1.11.2` 当前已锁定但曾作为 indirect | P0-07A 只补齐已锁定依赖或探针所需 checksum，不升级版本 |
| `internal/testinfra/` | PostgreSQL/Redis 等测试依赖基础设施 | P0-07 测试不要求启动外部服务；Fake Provider 必须本地可控 |

### 6. 新上下文执行前的最小检查

开始任何实现前，Pi 应完成以下动作并把结果作为本阶段事实起点：

```bash
pwd
git status --short
sed -n '1,260p' docs/plan007v1
sed -n '1,260p' docs/ARCHITECTURE.md
sed -n '1,260p' docs/project-status.md
sed -n '1,260p' docs/implementation-plan.md
rg -n 'type (TenantContext|Runner|Responder|EchoResponder)|MODEL_PROVIDER|trpc-agent-go' \
  trpcservice cmd go.mod
```

读取结果后，先更新本文件的 P0-07 验收矩阵状态；不要把旧会话中“已经修改过”当作当前工作区事实。确认当前只推进一个子阶段，再按照本文件的 `in-scope`、文件范围和 targeted 命令工作。任何测试 skip、依赖缺失或 API 路径不确定都记录为 `blocked/not verified`，不要用重复全量测试或放宽断言解决。

### 7. 关键固定决策

以下决策来自 [`ARCHITECTURE.md`](ARCHITECTURE.md) 和 [`implementation-plan.md`](implementation-plan.md)，P0-07 必须遵守：

- Worker 最终无状态，不依赖 sticky session；P0-07 Runtime 不持有 Session/Memory 事实状态。
- PostgreSQL 是业务事实源，Redis 是协调/热数据层；P0-07 不把任何数据库或 Redis 读写塞进 Runtime。
- 所有合法执行入口都必须携带并再次校验 `TenantContext`；不信任请求 body、header、外部 IM payload 或模型输出中的 tenant ID。
- `context.Context` 是取消和 deadline 的传播入口；取消后必须停止/取消下游并排空框架事件，不能留下阻塞发送 goroutine。
- tRPC-Agent-Go 依赖和类型隔离在 `trpcservice/agent`；平台公开接口只暴露本项目纯数据类型。
- `MODEL_PROVIDER=echo` 是显式开发 fallback；`MODEL_PROVIDER=runner` 初始化或执行失败时直接报错，不能自动回退 Echo。
- P0-07 不新增 Gateway、Worker、Queue、Outbox、Repository、IM Adapter、Secret Manager、Guardrail 治理或任意 Shell/代码执行。

## 目标

在不改变租户、Channel、Storage 契约的前提下，引入一个平台自有的 Agent Runtime 边界：内部使用已锁定的 `trpc.group/trpc-go/trpc-agent-go v1.11.2` Runner，向平台其他包只暴露本项目定义的 `AgentFactory`、`AgentRuntime`、`AgentInput`、`AgentResult` 和 `RunnerEvent`。

P0-07 完成后：

- `platform` 可以通过 `TenantContext` 调用真实 Runner Adapter。
- `EchoResponder` 仍然存在，但只能作为明确选择的本地/开发 fallback，不能在真实 Runner 失败时自动降级。
- Runtime 处理 `context.Context` 取消，并保证 Runner Event channel 在成功、错误、取消路径上被正确消费和结束。
- 测试使用 Fake Provider，不读取或依赖真实模型 API Key。
- 不实现 Gateway、Worker、Job Queue、Outbox、Repository、IM 协议或后续治理基础设施。

默认运行策略锁定为：`MODEL_PROVIDER=runner` 使用真实 Runner Adapter；`MODEL_PROVIDER=echo` 才使用 `EchoResponder`。Fake Provider 仅存在于测试依赖中，不作为生产配置选项。

本计划采用“单子阶段、单阻塞、可关闭”的执行方式。P0-07 只有在 P0-07A 至 P0-07D 全部关闭后才算完成，但任何已关闭子阶段不因后续子阶段未完成而重新打开。每轮执行只能处理一个子阶段；发现超出范围的需求先记录到 backlog，不自动扩大当前阶段。

## 执行控制总则

### 1. 阶段划分

| 子阶段 | 目标 | 允许范围 | 关闭证据 |
| --- | --- | --- | --- |
| P0-07A | Runtime 公共边界和框架 API 探针 | 平台类型、Factory/Runtime 契约、v1.11.2 API 最小编译探针、Fake Provider 基础 fixture | 契约编译通过，边界无框架类型泄漏，fixture 可单独运行 |
| P0-07B | Agent Runtime 核心执行 | Factory 校验、Runner 调用、成功/错误/取消、Event drain 和生命周期 | agent 包 targeted tests、race、取消和 drain 失败诊断通过 |
| P0-07C | 平台桥接和显式模式选择 | `platform` Runtime bridge、`cmd` 的 runner/echo 选择、既有 Echo 回归 | platform/入口 targeted tests 通过，runner 初始化失败不降级 |
| P0-07D | P0-07 阶段关闭回归 | 受影响包全量回归、静态边界检查、完整关闭报告 | go test、race、vet、gofmt 和边界检查均有实际结果 |

P0-07A 关闭前不得进入 P0-07B；P0-07B 关闭前不得接入 `platform` 或修改启动模式；P0-07C 关闭前不得宣称平台已使用真实 Runner。P0-07D 只在前三个子阶段都已关闭后执行一次，不用于反复诊断单项失败。

### 2. 范围和状态规则

- 每个子阶段必须明确 `in-scope`、`out-of-scope`、变更文件、targeted 命令和关闭条件。
- 每个子阶段最多维护一个当前阻塞项；遇到第二个问题先暂停扩展，合并为现有阻塞项或放入 backlog。
- “代码存在”“可编译”“普通测试通过”不能单独标记完成；`[no test files]` 不能计入行为验收。
- 测试因未设置 `MODEL_PROVIDER`、缺少依赖或外部服务而跳过时，状态为 `blocked/not verified`，不得标记 `passed`。
- 只有明确属于当前子阶段的测试失败才在本轮修复；不通过全量测试来掩盖当前 targeted 验收缺口，也不重复运行与当前问题无关的全量测试。
- 任何修改公共接口、TenantContext 或框架适配边界的行为，都必须保留失败诊断和回归测试；不能只调整断言使测试变绿。
- 每个子阶段关闭时输出：变更文件、实际命令及结果、未运行/blocked 项、已知限制、唯一下一阶段入口。未关闭时保持 `in_progress`，不能以“本轮完成”替代阶段关闭。

### 3. 固定验收矩阵

开始 P0-07 时建立并持续更新以下矩阵；每个格子只能填写 `passed`、`failed`、`blocked` 或 `not started`，不能用“基本完成”：

| 验收项 | 单元/契约 | 真实 Runner/Fake | race | 状态 | 阻塞/证据 |
| --- | --- | --- | --- | --- | --- |
| P0-07A 公共 Runtime 边界 | passed | passed (Fake) | passed | passed | `go test ./trpcservice/agent`; 纯数据接口、复制语义和框架构造已验证 |
| P0-07A v1.11.2 API 编译探针 | passed | passed | passed | passed | 已确认 `runner`, `event`, `agent/llmagent`, `model`; 仅使用锁定 `v1.11.2` |
| P0-07B Factory 与租户隔离 | passed | passed (Fake) | passed | passed | Factory/Runtime 校验 TenantID、AgentAppID、ConfigVersion；未接入外部模型 API |
| P0-07B 成功/错误结果映射 | passed | passed (Fake) | passed | passed | 成功输出、usage、finish type、provider/framework 错误事件已覆盖 |
| P0-07B Context 取消 | passed | passed (Fake) | passed | passed | 主动取消传播至 Fake Provider；返回 `context.Canceled` |
| P0-07B Event channel drain | passed | passed (Fake) | passed | passed | 成功/取消 drain；未关闭 channel 在 bounded timeout 内返回诊断 |
| P0-07C platform bridge | passed | passed (Fake) | passed | passed | `RuntimeResponder` 从已验证 TenantContext 构造纯数据 AgentInput |
| P0-07C runner/echo 显式选择 | passed | passed (Fake) | passed | passed | `MODEL_PROVIDER=echo|runner` 入口测试通过；runner 无外部 Provider 时 fail-closed，绝不回退 Echo |
| P0-07D 全量回归与边界检查 | passed | passed (Fake) | passed | passed | `go test ./...`, `go test ./... -race`, `go vet ./...`, gofmt、边界 rg 均通过；真实模型 API 不在本阶段验收范围 |

矩阵中的“真实 Runner/Fake”必须说明使用的是本地 Fake Provider 还是实际 Runner 构造；Fake Provider 通过不等于真实模型服务通过。P0-07 不要求真实模型 API，也不允许为验证方便读取或输出 API Key。

## 前置依赖

- P0-01：Go 依赖、CI、format、lint、test、vet 基线。
- P0-02：`TenantContext`、租户/Agent 配置领域模型和权限边界。
- P0-03：Session/Event 等平台领域类型，可用于输入历史和输出事件映射。
- P0-04：存储契约和稳定 ID 语义；P0-07 不实现具体存储。
- P0-05、P0-06 的迁移和协调能力不是 P0-07 的运行时依赖，不在本任务中接入。

## 1. 当前代码状态

- [`trpcservice/agent/agent.go`](../trpcservice/agent/agent.go) 当前只有说明性注释，没有 AgentFactory、AgentRuntime 或 tRPC-Agent-Go 调用。
- [`trpcservice/platform/platform.go`](../trpcservice/platform/platform.go) 当前包含同步 `platform.Runner`、`Responder` 接口和 `EchoResponder`；Runner 读取 `MemoryStore` 历史后调用 Responder，并写回平台消息和基础 Event。
- 当前 `EchoResponder` 是真实可运行的本地联调实现，不能删除。
- [`cmd/trpc-service/main.go`](../cmd/trpc-service/main.go) 固定注入 `platform.EchoResponder{}`；虽然 `go.mod` 已锁定 `trpc.group/trpc-go/trpc-agent-go v1.11.2`，但该依赖目前没有作为平台 Runtime 使用。
- [`trpcservice/tool/tool.go`](../trpcservice/tool/tool.go) 目前是占位包；P0-07 只需要为 Runtime 保留平台无关的 Tool 依赖边界，不实现工具治理、审批或危险工具。
- 当前 `platform` 测试覆盖 Echo Runner 的幂等、错误和租户隔离，但没有真实 Runner Adapter、Fake Provider、Event channel drain 或取消测试。
- 当前 Web/Channel/Storage 包不应依赖 tRPC-Agent-Go 类型；P0-07 的框架依赖只能进入 `trpcservice/agent` 的实现文件。

## 2. 需求与现有代码的差距

1. 没有真实 tRPC-Agent-Go Runner 构造和调用层，现有 `Responder` 只支持 Echo。
2. 没有稳定的平台 Runtime 输入/输出模型，无法隔离框架版本变化。
3. `TenantContext` 尚未进入 Agent Factory 和 Runtime 的强制调用路径；需要在构造和执行时校验租户、AgentApp、Binding、配置版本和权限。
4. 没有模型/Provider 注入点，无法在无 API Key 的条件下验证成功、失败、取消和事件行为。
5. 没有定义 Event channel 的所有权：框架拥有的 channel 不能由平台关闭，平台必须持续消费到关闭；平台自建的桥接 channel 才由平台关闭。
6. 当前主进程没有 Runtime/Fallback 选择逻辑，必须增加显式模式选择，否则真实 Runner Adapter 不会成为可运行路径。
7. 当前没有对 Runner goroutine、Provider 阻塞和 Context 取消的生命周期测试。

## 3. 分阶段实施步骤

### P0-07A：固定 Runtime 边界和 v1.11.2 API 探针

在 `trpcservice/agent` 中定义只包含标准库、本项目 `tenant`/领域类型和本项目配置类型的公开接口。接口签名中禁止出现 tRPC-Agent-Go 的 `Runner`、`Event`、Agent、Model、Tool 或任何框架包类型。

P0-07A 只建立适配层的边界和最小编译探针，不实现完整执行链：

- 确认本项目 `AgentSpec`、`TenantContext`、历史消息和当前输入可以映射到 v1.11.2 所需的请求形状；具体转换实现留到 P0-07B。
- 确认框架事件、错误和 stop/cancel API 的实际类型及所有权；事件转换和 drain 留到 P0-07B。
- 先定义稳定错误分类和 `TenantContext` 防御性复制要求，不在本阶段接入 Provider、Tool 或模型调用。

### P0-07A：定义 Agent 配置和依赖注入

在 `trpcservice/agent` 内定义与框架无关的 Agent 配置和依赖集合：

- Agent 标识、租户标识、版本、模型 Provider 名称、System Prompt、Tool 配置引用。
- `RuntimeDependencies` 包含 Provider 工厂、平台 Tool 注册表/桥接器和可选 clock；不包含 Secret 明文。
- 真实 Provider 的具体构造只存在于 `agent` 包内部；P0-07 不读取真实密钥，也不新增 Secret Manager。
- Factory 每次构造 Runtime 时验证 `TenantContext` 与 AgentSpec 的 TenantID/AgentAppID/配置版本一致；不允许跨租户复用可变 Agent 配置。
- Runtime 可以复用只读的 immutable Agent 配置，但不持有 Session、Memory 或数据库状态；这些属于后续 Execution/Repository 任务。

### P0-07B：实现 tRPC-Agent-Go Runner Adapter

在 `trpcservice/agent` 内根据 v1.11.2 实际 API 完成内部构造：

- 通过 Factory 创建租户/Agent 版本对应的 Runner/Agent。
- 将 Fake Provider 以框架要求的 Provider 形状注入，真实 Provider 仅保留生产构造入口。
- 将 `context.Context` 从 `AgentRuntime.Run` 传递到 Runner、Provider、Tool 和 Event 消费者。
- 不把框架对象存进 Tenant、Channel、Storage 或平台公共结构。
- 增加编译期适配点/封装函数，使未来框架 API 变化局限在 `trpcservice/agent` 内。

### P0-07B：实现 Event 消费和生命周期规则

锁定以下所有权规则：

- 框架返回的 Event channel 由框架拥有；Adapter 不调用 `close`。
- Adapter 是 channel 的唯一消费者，按事件顺序转换并收集/转发 `RunnerEvent`。
- 正常完成：消费直到 channel 关闭，再返回结果。
- Provider/Runner 返回错误：记录已有事件，继续消费直到框架 channel 关闭，再返回稳定错误。
- `ctx.Done()`：取消下游 Runner/Provider，继续消费直到 Runner 关闭 Event channel；必要时使用独立的内部 drain completion 信号等待生产者退出，不能直接返回并遗留阻塞发送 goroutine。
- 如果框架 API 提供显式 close/stop 方法，只调用其公开的停止方法，不直接关闭框架 channel。
- Adapter 自己创建的桥接 channel 必须由创建方在唯一出口关闭，并通过 `defer`/completion 等待保证不会重复关闭。
- Event 消费有明确的超时/错误策略；不得用无限等待掩盖 Runner 永不关闭 channel 的实现错误，测试中必须能检测并报告该错误。

本子阶段不实现 Worker shutdown、Job drain 或服务级 graceful shutdown；这里只保证一次 Runtime 执行的 goroutine 和 Event 生命周期。

### P0-07C：保留并显式接入 Echo fallback

- 保留现有 `platform.EchoResponder` 的行为和测试。
- 在 `platform` 中增加一个轻量 Runtime-to-Responder bridge，负责把平台消息历史和经过验证的 `TenantContext` 组装为 `agent.AgentInput`，再调用 `agent.AgentRuntime`。
- Bridge 不包含 tRPC-Agent-Go 类型；它只依赖 `agent` 的平台接口。
- 在 `cmd/trpc-service/main.go` 增加明确的运行模式选择：`runner` 构造真实 Adapter，`echo` 构造现有 EchoResponder；真实 Runner 初始化失败时直接启动失败或返回明确错误，禁止静默降级到 Echo。
- 本任务只保持当前同步 `platform.Runner` 兼容形状；不把执行改造成 Job/Worker，也不改变 Web/Channel 路由。

### P0-07B：用 Fake Provider 补齐 Runtime 测试

Fake Provider 必须完全在测试代码或 `agent` 测试专用支持文件中定义：

- 可以返回确定文本和有序 Tool/assistant 事件。
- 可以阻塞到收到 Context 取消。
- 可以返回模型错误、Provider 错误和预设 Event stream。
- 记录是否收到取消、是否完整消费、是否发生重复关闭/发送阻塞。
- 测试不得读取 API Key 环境变量、访问外部网络或依赖真实模型服务。

### 4. 子阶段入口、退出和阻塞处理

#### P0-07A：边界先行

`P0-07A` 只定义平台边界并确认 v1.11.2 的最小可用 API。先查阅本地 module cache 或使用 `go doc`/最小编译探针确认 Runner、Model/Provider、Event 和 stop/cancel API；不得凭印象一次性实现全部 Adapter。该阶段不修改 `platform`、`cmd`、Web、Channel 或 Storage，不接入真实模型，不实现 Event drain。

关闭条件：

- `AgentSpec`、`AgentInput`、`AgentResult`、`RunnerEvent`、`AgentFactory`、`AgentRuntime` 的公开定义通过编译。
- 公开接口和 `tenant`、`channels`、`storage` 源码不出现 tRPC-Agent-Go 类型。
- Fake Provider 的最小 fixture 能产生成功、错误和取消三类可控结果，但不宣称 Runtime 已完成。
- P0-07A 矩阵必需项全部为 `passed`；若 API 探针失败，只记录一个阻塞项并修正探针或适配边界。

#### P0-07B：先关闭 Runtime，再做平台接入

`P0-07B` 只修改 `trpcservice/agent` 及其测试。按“Factory 校验 -> 单次成功 -> 错误映射 -> 取消 -> Event drain -> 并发 race”的顺序推进。任何失败先保留每次调用的 error、事件数量、事件关闭状态和取消信号，不用放宽断言来结束阶段。

关闭条件：

- 有效 `TenantContext + AgentSpec` 才能构造并运行；租户、AgentApp、版本和权限不匹配均被拒绝。
- Fake Provider 成功、模型/Tool 错误、主动取消和 deadline exceeded 都有可区分结果。
- 成功、错误、取消路径均消费到框架 channel 关闭；Adapter 不关闭框架拥有的 channel。
- 不关闭 channel 的故障 fake 能在有界 drain timeout 内返回明确错误，且无遗留发送阻塞或 goroutine 泄漏证据。
- `go test ./trpcservice/agent -race` 及该包 targeted tests 通过后，才将 P0-07B 标记为 `verified`。

#### P0-07C：最小平台桥接

`P0-07C` 才允许修改 `platform` 和 `cmd/trpc-service/main.go`。只把既有同步链路接到稳定的 `agent` 接口，并增加显式 `runner|echo` 选择；不改变 Session、Storage、Web、Channel 或错误之外的既有行为。

关闭条件：

- `MODEL_PROVIDER=echo` 保持既有测试行为。
- `MODEL_PROVIDER=runner` 通过 Runtime bridge 运行；Runner 初始化失败直接报错，绝不自动切换 Echo。
- Bridge 从已验证的 `TenantContext` 获取租户，不从请求字段/header/模型输出推断租户。
- platform targeted tests、入口构造测试和既有 Echo 回归通过；P0-07C 不因 P0-07D 全量回归尚未执行而提前标记 P0-07 完成。

#### P0-07D：一次性阶段关闭

前三个子阶段关闭后，执行本计划第 6 节的最终回归和静态边界检查。若最终回归暴露问题，P0-07D 保持 `in_progress`，只修复 P0-07D 或明确回归到对应子阶段；不重复运行已经通过且未受影响的外部依赖测试。

阶段关闭报告模板：

```text
阶段：P0-07A/P0-07B/P0-07C/P0-07D
状态：closed 或 in_progress
变更文件：...
已通过命令：...
未运行/blocked：...
已知限制：...
下一阶段入口：...
```

## 4. Go 接口和数据结构

以下是平台稳定边界，具体 tRPC-Agent-Go 类型只允许出现在 `trpcservice/agent` 的未导出实现中。

```go
package agent

import (
    "context"

    "github.com/liuzengh/trpc-agent-service/trpcservice/tenant"
)

type AgentSpec struct {
    TenantID       string
    AgentAppID     string
    Version        int64
    Name           string
    ModelProvider  string
    SystemPrompt   string
    ToolPolicyRef  string
    GuardrailRef   string
}

type AgentInput struct {
    TenantContext tenant.TenantContext
    Agent         AgentSpec
    History       []Message
    Input         Message
}

type Message struct {
    ID        string
    Role      string
    Content   string
    CreatedAt time.Time
}

type RunnerEvent struct {
    Sequence  int64
    Type      string
    Role      string
    Content   string
    ToolName  string
    ErrorType string
    Metadata  map[string]string
}

type AgentResult struct {
    Text       string
    Events     []RunnerEvent
    Usage      Usage
    FinishType string
}

type Usage struct {
    InputTokens  int64
    OutputTokens int64
}

type AgentFactory interface {
    Build(context.Context, tenant.TenantContext, AgentSpec) (AgentRuntime, error)
}

type AgentRuntime interface {
    Run(context.Context, AgentInput) (AgentResult, error)
}
```

实施时应按现有项目类型做最小调整：如果平台已有消息类型可复用，则在 `agent` 包内建立显式转换，而不是让 `tenant`、`channels` 或 `storage` 依赖框架类型。`time.Time` 等标准库字段需要在正式实现中补齐 import；示例表达的是边界，不是要求直接复制的最终代码。

建议增加内部依赖接口，但不导出框架类型：

```go
type Provider interface {
    Complete(context.Context, ProviderRequest) (ProviderResponse, error)
}

type ToolInvoker interface {
    Invoke(context.Context, tenant.TenantContext, string, []byte) (ToolResult, error)
}

type RuntimeDependencies struct {
    Provider     Provider
    ToolInvoker  ToolInvoker
}
```

`ProviderRequest`、`ProviderResponse`、`ToolResult` 只能使用本项目的纯数据类型。真实 tRPC-Agent-Go Provider/Tool 的转换实现必须封装在 `agent` 内部。若 v1.11.2 Runner API 不允许直接注入上述抽象，则增加未导出的 framework bridge，不修改公开接口。

平台桥接建议保持如下方向：

```go
type RuntimeResponder struct {
    Factory agent.AgentFactory
    Spec    func(tenant.TenantContext, platform.Tenant) agent.AgentSpec
}

func (r RuntimeResponder) Respond(
    ctx context.Context,
    t platform.Tenant,
    history []platform.Message,
    input string,
) (string, error)
```

`RuntimeResponder` 从 `tenant.FromContext(ctx)` 获取并验证 Context；缺少或不匹配 Context 时拒绝执行，不从请求字段推断租户。平台层只处理自己的 `Tenant`/`Message` 与 agent 层纯数据，不能导入 `trpc.group/trpc-go/trpc-agent-go`。

## 5. 分阶段测试与验收矩阵

测试按子阶段执行，禁止每次局部修改后反复运行全量回归：

- P0-07A：先运行接口/编译探针和边界静态检查，再运行 `agent` 包中与 fixture 相关的 targeted tests。
- P0-07B：先单测单次 Run，再测错误、取消、drain timeout，最后运行 `go test ./trpcservice/agent -race`。
- P0-07C：只运行 `platform`、`cmd` 相关测试和 Echo 回归；Runner 模式测试失败时不得改成 Echo 通过。
- P0-07D：前三阶段关闭后才运行一次 `go test ./...`、`go test ./... -race`、`go vet ./...` 和格式检查。

每项测试记录实际命令、是否执行、退出结果和证据文件。未设置真实模型 API Key 不构成阻塞，因为 P0-07 必须使用 Fake Provider；需要外部服务的测试不属于本阶段，不能通过连接真实服务扩大范围。

### Factory 和租户隔离

1. 有效 `TenantContext + AgentSpec` 能构造 Runtime。
2. 缺失/非法 TenantContext 被拒绝。
3. TenantID 不一致被拒绝。
4. AgentAppID、版本或 Backend/权限上下文不匹配被拒绝。
5. 两个租户使用不同 AgentSpec 时，Provider 请求、System Prompt 和事件不能串用。
6. Runtime 不修改调用方的 TenantContext、权限切片、历史消息或输入消息。

### 成功和错误

7. Fake Provider 返回固定结果，`AgentResult.Text`、Usage 和事件顺序正确。
8. Fake Provider 返回模型错误，Runtime 返回稳定错误并保留已产生的错误/失败事件。
9. Fake Tool 返回结果，Tool started/completed 事件能转换为平台 `RunnerEvent`；不测试后续 Tool Policy 规则。
10. 空输出、非法事件类型、Provider 返回不完整结果时有明确错误，不产生伪造成功结果。

### Context 取消

11. Run 开始前 Context 已取消，Provider 不被执行或立即收到取消，Runtime 返回 `context.Canceled` 语义。
12. Run 执行中取消，Fake Provider 阻塞并在取消后退出；Runtime 返回且不存在遗留 goroutine。
13. Tool/Provider 阻塞时取消能传播到下游，不能等待无限时间。
14. deadline exceeded 与主动 canceled 能区分到 `ErrorType`/返回错误链中。

### Event channel 生命周期

15. 正常完成时 Runtime 消费到 channel close，所有事件顺序和数量正确。
16. Provider/Runner 失败时已产生事件仍被消费，channel 最终关闭后再返回。
17. 取消时 Runtime 触发下游取消并继续 drain，Fake Provider 的发送端不会因无人消费而阻塞。
18. Adapter 不关闭框架拥有的 channel；自有桥接 channel 只关闭一次。
19. Runner 永不关闭 channel 的故障 fake 能在规定 drain timeout 内失败并给出明确诊断。
20. `go test -race` 下并发运行多个租户/多个 Runtime 不出现数据竞争、重复关闭或共享可变配置。

### Echo fallback 和集成边界

21. `MODEL_PROVIDER=echo` 仍通过现有 `EchoResponder` 返回原行为。
22. `MODEL_PROVIDER=runner` 使用 Runtime bridge；Runner 初始化失败不自动回退 Echo。
23. 当前 `platform.Runner` 的幂等、历史写回、audit 和既有租户边界测试继续通过。
24. 不需要启动 Web/IM、PostgreSQL、Redis 或真实模型服务即可完成 P0-07 测试。

## 6. 分阶段验收命令

所有命令在仓库根目录执行。先执行当前子阶段的 targeted 命令；只有 targeted 项全部通过，才进入下一阶段。命令被跳过、依赖缺失或环境不满足时，必须记录为 `blocked/not verified`。

### P0-07A

```bash
# 只补齐并确认已锁定版本，不升级依赖
go mod download trpc.group/trpc-go/trpc-agent-go@v1.11.2
go list -m trpc.group/trpc-go/trpc-agent-go
go list -f '{{.ImportPath}}' 'trpc.group/trpc-go/trpc-agent-go/...'
```

将上一步实际列出的 Runner/Model/Event 包路径传给 `go doc` 做 API 探针；不要预先假定包名为 `runner`：

```bash
go doc <已确认的 Runner 包路径>
go doc <已确认的 Model/Provider 包路径>
```

如果依赖下载、`go list` 或 API 探针失败，P0-07A 标记为 `blocked/not verified`，只处理依赖解析或 API 路径确认，不凭印象实现 Adapter，也不进入 P0-07B。API 探针通过后再运行：

```bash
go test ./trpcservice/agent -run 'Test(Agent|Runtime|Provider|Boundary)' -count=1
```

如本地版本的包路径不同，只调整探针命令和 `agent` 内部 bridge，不扩展到其他业务包。边界检查：

```bash
rg -n 'trpc\.group/trpc-go/trpc-agent-go' trpcservice/tenant trpcservice/channels trpcservice/storage
```

该搜索应无输出；若有输出，只修复 P0-07A 的公开边界，不修改后续实现。

### P0-07B

```bash
go test ./trpcservice/agent -run 'Test(Factory|Runtime|Run|Cancel|Drain|Fake)' -count=1 -v
go test ./trpcservice/agent -race -count=1
```

必须实际覆盖成功、模型/Tool 错误、主动取消、deadline、已关闭 channel、错误后 drain 和不关闭 channel 的 bounded timeout。测试不得依赖外部网络或真实 API Key。

### P0-07C

```bash
go test ./trpcservice/platform -run 'Test(Runtime|Echo|Tenant|Runner)' -count=1 -v
go test ./trpcservice/platform ./cmd/trpc-service -count=1
```

如果 `cmd/trpc-service` 没有可测试包，不得把 `[no test files]` 当作入口模式验收通过；应以可测试的构造函数或最小入口单测补足，仍只修改 P0-07C 范围。

### P0-07D

仅在 P0-07A、P0-07B、P0-07C 的矩阵项全部为 `passed` 后执行一次：

```bash
go test ./...
go test ./... -race
go vet ./...
test -z "$(gofmt -l .)"
```

最终还要完成以下静态边界检查：

```bash
rg -n 'trpc\.group/trpc-go/trpc-agent-go' trpcservice/tenant trpcservice/channels trpcservice/storage
rg -n 'MODEL_PROVIDER|EchoResponder|AgentRuntime|AgentFactory' trpcservice/agent trpcservice/platform cmd/trpc-service
```

第一条必须无输出；第二条用于记录模式选择和依赖方向，不以字符串命中数量代替行为测试。`go list -deps ./trpcservice/agent` 只能证明依赖可达，不能替代 Runtime 行为、取消和 Event drain 验收。

## 7. 风险和回滚方案

| 风险 | 缓解 | 回滚 |
| --- | --- | --- |
| v1.11.2 Runner API 与预期不同 | 实现前只在 `agent` 包内确认实际 API；用未导出 bridge 隔离；不升级框架版本 | 保留平台接口和 Echo，移除内部 Adapter 实现，不改 tenant/channel/storage |
| Event channel 所有权判断错误 | 明确“框架 channel 不关闭、Adapter 只消费”；Fake 覆盖正常/错误/取消/不关闭 | 恢复 Echo 模式；禁止启用真实 Runner，保留失败测试定位 |
| 取消后 Provider 或 Runner goroutine 泄漏 | Fake Provider 可控阻塞、completion 信号、race/泄漏检查、bounded drain 诊断 | 回退到 EchoResponder，同步关闭 Runner 模式，不影响已完成 P0-06 |
| TenantContext 未传递或跨租户复用 Runtime | Factory/Bridge 双重校验 TenantID、AgentAppID、版本和权限；Runtime 不缓存可变 Context | 禁用 Runner 模式，使用显式 Echo fallback；修复后再重新启用 |
| 真实 Runner 初始化失败静默降级 | `MODEL_PROVIDER=runner` 失败即报错；Echo 只由显式配置选择 | 将本地启动配置切换为 `MODEL_PROVIDER=echo`，不修改数据和协调层 |
| 平台输出语义被框架事件污染 | 使用本项目 `RunnerEvent` 和稳定错误映射；框架类型不得越过 agent 包 | 恢复原 `Responder` 路径，保留 adapter 单元测试 |
| 为 P0-07 提前引入 Tool/Gateway/Storage 基础设施 | Runtime 只使用注入的纯数据 Provider/ToolInvoker，不新增队列、数据库或 IM 依赖 | 删除超出范围的依赖和目录，保留纯 Runtime 契约 |

回滚只涉及 P0-07 允许范围内的 Runtime、平台桥接、入口模式选择、测试和必要依赖；不得回滚或改写 P0-01 至 P0-06 的迁移、协调逻辑或领域契约。不得执行 `git commit`、`git push`、生产访问或读取/输出任何密钥、Token、密码和生产日志。

## 8. 分阶段文件范围

文件范围是硬边界。未列入当前子阶段的文件不能因为测试输出或“顺手完善”而修改；确需跨边界修改时，先记录为阻塞或 backlog，并说明为什么公共契约无法在原边界内解决。

### P0-07A 文件范围

允许新增或修改：

- `trpcservice/agent/agent.go`：平台 Runtime 接口、纯数据类型和最小依赖定义；只完成到可编译边界。
- `trpcservice/agent/agent_test.go`：API 探针、契约、Fake Provider fixture 和边界测试。
- `go.mod`、`go.sum`：仅补齐已锁定的 v1.11.2 直接依赖，不升级版本、不引入模型厂商 SDK。

禁止修改 `platform`、`cmd`、`tenant`、`channels`、`storage`、`migrations` 和后续基础设施目录。

### P0-07B 文件范围

允许新增或修改：

- `trpcservice/agent/agent.go` 或 `trpcservice/agent/` 内与 Runtime/bridge 直接相关的实现文件。
- `trpcservice/agent/agent_test.go` 或 `trpcservice/agent/` 内的 Fake Provider、取消和 Event drain 测试文件。
- `trpcservice/tool/tool.go`：只有在 Adapter 需要纯数据 `ToolInvoker` 边界时，才做最小接口修改；不实现治理。

该阶段禁止修改 `platform`、`cmd`、Web/Channel/Storage 和数据库；不得创建 Gateway、Worker、Queue 或 Outbox。

### P0-07C 文件范围

允许新增或修改：

- `trpcservice/platform/platform.go`、`trpcservice/platform/platform_test.go`：Runtime-to-Responder bridge 和既有行为回归。
- `cmd/trpc-service/main.go`：显式 `MODEL_PROVIDER=runner|echo` 选择及初始化错误处理。
- P0-07B 已关闭的 `trpcservice/agent/` 文件：只有平台桥接暴露出已证明的 Runtime 契约缺口时，才能做兼容性修复，并回到 P0-07B 重新验证。

### P0-07D 文件范围

原则上只修改测试、格式和 P0-07 回归暴露的缺陷。若缺陷属于 P0-07A/B/C 的行为边界，先将对应子阶段重新标记为 `in_progress`，修复并重新关闭后再继续 P0-07D；不在 P0-07D 中横向扩展功能。

### 明确不修改

- `trpcservice/tenant/`、`trpcservice/channels/`、`trpcservice/storage/`、`migrations/`。
- `trpcservice/web/` 的 webhook 验签、HTTP 路由和协议行为；Gateway/IM 改造属于后续任务。
- `trpcservice/gateway/`、`worker/`、`queue/`、`outbox/`、`auth/`、`governance/`、`telemetry/`、`vector/`、`object/` 等后续目录。
- 生产配置、Secret Manager、外部 API Key、数据库数据和部署基础设施。

实施完成后，只在任务状态需要同步时更新权威计划中的 P0-07 状态；不新增临时计划文件，不执行提交或推送。
