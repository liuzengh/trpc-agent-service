import { BranchUp } from 'reicon-react/icons/BranchUp'
import { BrowserCode } from 'reicon-react/icons/BrowserCode'
import { CloudStorage } from 'reicon-react/icons/CloudStorage'
import { CloudUpload } from 'reicon-react/icons/CloudUpload'
import { Copy } from 'reicon-react/icons/Copy'
import { Grid } from 'reicon-react/icons/Grid'
import { History } from 'reicon-react/icons/History'
import { Layers } from 'reicon-react/icons/Layers'
import { Link } from 'reicon-react/icons/Link'
import { LinkBroken } from 'reicon-react/icons/LinkBroken'
import { Play } from 'reicon-react/icons/Play'
import { Plug } from 'reicon-react/icons/Plug'
import { Plus } from 'reicon-react/icons/Plus'
import { Power } from 'reicon-react/icons/Power'
import { Refresh } from 'reicon-react/icons/Refresh'
import { Route } from 'reicon-react/icons/Route'
import { Search } from 'reicon-react/icons/Search'

import { uiIcon } from './IconFactory'

export const BranchIcon = uiIcon(BranchUp)
export const BrowserCodeIcon = uiIcon(BrowserCode)
export const CloudStorageIcon = uiIcon(CloudStorage)
export const UploadCloudIcon = uiIcon(CloudUpload)
export const CopyIcon = uiIcon(Copy)
export const GridIcon = uiIcon(Grid)
export const HistoryIcon = uiIcon(History)
export const MemoryIcon = uiIcon(History)
export const LayersIcon = uiIcon(Layers)
export const LinkIcon = uiIcon(Link)
export const UnlinkIcon = uiIcon(LinkBroken)
export const PlayIcon = uiIcon(Play)
export const PlugIcon = uiIcon(Plug)
export const PlusIcon = uiIcon(Plus)
export const PowerIcon = uiIcon(Power)
export const RefreshIcon = uiIcon(Refresh)
export const RouteIcon = uiIcon(Route)
export const SearchIcon = uiIcon(Search)
export const GitBranchIcon = uiIcon(BranchUp)
