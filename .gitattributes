# Shell scripts must always use LF, otherwise bash fails with
# "set: pipefail: invalid option name" on CRLF checkouts (e.g. Windows).
*.sh text eol=lf
*.go text eol=lf
*.sql text eol=lf

# GitHub Actions workflows
*.yml text eol=lf
