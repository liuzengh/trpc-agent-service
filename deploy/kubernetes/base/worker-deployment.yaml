apiVersion: apps/v1
kind: Deployment
metadata:
  name: trpc-agent-service-worker
  labels:
    app.kubernetes.io/name: trpc-agent-service
    app.kubernetes.io/component: worker
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: trpc-agent-service
      app.kubernetes.io/component: worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: trpc-agent-service
        app.kubernetes.io/component: worker
    spec:
      terminationGracePeriodSeconds: 45
      containers:
        - name: worker
          image: trpc-agent-service:0.1.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: trpc-agent-service-config
            - secretRef:
                name: trpc-agent-service-secrets
          env:
            - name: TRPC_AGENT_SERVICE_ROLE
              value: worker
            - name: TRPC_AGENT_SERVICE_HTTP_ADDR
              value: :8080
            # envFrom must not expose control-plane credentials to workers.
            - name: TRPC_AGENT_SERVICE_ADMIN_TOKEN
              value: ""
            - name: TRPC_AGENT_SERVICE_OPERATOR_TOKEN
              value: ""
            - name: TRPC_AGENT_SERVICE_AUDITOR_TOKEN
              value: ""
            - name: TRPC_AGENT_SERVICE_WORKER_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: TRPC_AGENT_SERVICE_POSTGRES_DSN
              valueFrom:
                secretKeyRef:
                  name: trpc-agent-service-secrets
                  key: TRPC_AGENT_SERVICE_POSTGRES_DSN
            - name: TRPC_AGENT_SERVICE_REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: trpc-agent-service-secrets
                  key: TRPC_AGENT_SERVICE_REDIS_URL
          volumeMounts:
            - name: platform-secrets
              mountPath: /var/run/secrets/trpc-agent-service
              readOnly: true
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
      volumes:
        - name: platform-secrets
          secret:
            secretName: trpc-agent-service-secrets
