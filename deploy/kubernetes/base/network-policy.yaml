apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: trpc-agent-service-ingress
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: trpc-agent-service
      app.kubernetes.io/component: gateway
  policyTypes: [Ingress]
  ingress:
    - from:
        - podSelector: {}
        - namespaceSelector:
            matchLabels:
              trpc-agent.io/ingress: allowed
      ports:
        - {port: 8080, protocol: TCP}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: trpc-agent-worker-deny-ingress
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: trpc-agent-service
      app.kubernetes.io/component: worker
  policyTypes: [Ingress]
  ingress: []
