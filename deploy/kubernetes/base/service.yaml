apiVersion: v1
kind: Service
metadata:
  name: trpc-agent-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: trpc-agent-service
    app.kubernetes.io/component: gateway
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
