apiVersion: v1
kind: ConfigMap
metadata:
  name: trpc-agent-service-config
data:
  # Replace example endpoints before applying this overlay.
  TRPC_AGENT_SERVICE_OTEL_TRACES_ENDPOINT: otel-collector.staging.example.com:4317
  TRPC_AGENT_SERVICE_OTEL_METRICS_ENDPOINT: otel-collector.staging.example.com:4317
  TRPC_AGENT_SERVICE_QDRANT_ENDPOINTS: '{"knowledge":"https://qdrant.staging.example.com:6334"}'
